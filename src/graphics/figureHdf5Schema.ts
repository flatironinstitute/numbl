/** Version of the "numbl figure HDF5 layout" written into every exported file
 *  as the root `numbl_figure_version` attribute. Bump on incompatible schema
 *  changes; the layout is documented in docs/figure-hdf5-format.md. */
export const FIGURE_HDF5_VERSION = 1;
