/** Sentinel value representing the colon operator (:) in indexing. */
export const COLON_SENTINEL = Symbol("COLON");

/** Sentinel value representing the `end` keyword in indexing. */
export const END_SENTINEL = Symbol("END");
