// DO NOT EDIT — generated by scripts/build_runtime_snippets.ts.
// Re-run `npm run build:snippets` after editing any runtime/*.h
// or runtime/*.js file.

// Re-exports of every named export from each .js snippet, so the
// interpreter / `Builtin.call` definitions can import helpers
// directly and run the same code the emitters inline.
export * from "./cell/cell.js";
export * from "./indexing/loop_count.js";
export * from "./indexing/range_value.js";
export * from "./indexing/scalar_index.js";
export * from "./indexing/tensor_make_range.js";
export * from "./io/assert_fmt.js";
export * from "./io/disp_complex.js";
export * from "./io/disp_double.js";
export * from "./io/disp_struct.js";
export * from "./io/disp_tensor.js";
export * from "./io/disp_tensor_complex.js";
export * from "./io/error_fmt.js";
export * from "./io/format_complex.js";
export * from "./io/format_double.js";
export * from "./io/format_engine.js";
export * from "./io/fprintf.js";
export * from "./io/sprintf.js";
export * from "./io/warning_fmt.js";
export * from "./plot/plot_dispatch.js";
export * from "./system/cdiv.js";
export * from "./system/cscalar.js";
export * from "./system/deep_clone.js";
export * from "./system/isequal.js";
export * from "./system/tictoc.js";
export * from "./tensor/tensor.js";
export * from "./tensor/tensor_alloc.js";
export * from "./tensor/tensor_alloc_complex.js";
export * from "./tensor/tensor_alloc_nd.js";
export * from "./tensor/tensor_alloc_nd_complex.js";
export * from "./tensor/tensor_copy_complex.js";
export * from "./tensor/tensor_from_matrix.js";
export * from "./tensor/tensor_from_matrix_complex.js";
export * from "./tensor/tensor_from_row.js";
export * from "./tensor/tensor_from_row_complex.js";
export * from "./tensor_ops/tensor_cat.js";
export * from "./tensor_ops/tensor_cumulative.js";
export * from "./tensor_ops/tensor_diag.js";
export * from "./tensor_ops/tensor_diff.js";
export * from "./tensor_ops/tensor_dot.js";
export * from "./tensor_ops/tensor_elemwise_complex.js";
export * from "./tensor_ops/tensor_elemwise_real.js";
export * from "./tensor_ops/tensor_elemwise_real_fn.js";
export * from "./tensor_ops/tensor_eye.js";
export * from "./tensor_ops/tensor_fill_nd.js";
export * from "./tensor_ops/tensor_fill_square.js";
export * from "./tensor_ops/tensor_flip.js";
export * from "./tensor_ops/tensor_imag_all_zero.js";
export * from "./tensor_ops/tensor_linspace.js";
export * from "./tensor_ops/tensor_logical_real.js";
export * from "./tensor_ops/tensor_logspace.js";
export * from "./tensor_ops/tensor_meshgrid.js";
export * from "./tensor_ops/tensor_mtimes_complex.js";
export * from "./tensor_ops/tensor_mtimes_real.js";
export * from "./tensor_ops/tensor_norm.js";
export * from "./tensor_ops/tensor_ones_nd.js";
export * from "./tensor_ops/tensor_ones_square.js";
export * from "./tensor_ops/tensor_predicate.js";
export * from "./tensor_ops/tensor_reduce_complex.js";
export * from "./tensor_ops/tensor_reduce_real.js";
export * from "./tensor_ops/tensor_repmat.js";
export * from "./tensor_ops/tensor_reshape_nd.js";
export * from "./tensor_ops/tensor_reshape_nd_complex.js";
export * from "./tensor_ops/tensor_size.js";
export * from "./tensor_ops/tensor_sort_real.js";
export * from "./tensor_ops/tensor_transpose.js";
export * from "./tensor_ops/tensor_transpose_complex.js";
export * from "./tensor_ops/tensor_triangular.js";
export * from "./tensor_ops/tensor_unary_complex_math.js";
export * from "./tensor_ops/tensor_unary_real_math.js";
export * from "./tensor_ops/tensor_uniquetol.js";
export * from "./tensor_ops/tensor_var.js";
export * from "./tensor_ops/tensor_zeros_nd.js";
export * from "./tensor_ops/tensor_zeros_square.js";
export * from "./text/strcmp.js";

/** JS snippet bodies (with `export` keywords stripped), keyed by
 *  basename (e.g. `disp_double.js`). Inlined into emitted JS. The
 *  functions may reference host hooks (`$write`, …) as free
 *  variables that resolve to `globalThis.<name>` at call time. */
export const JS_SNIPPETS: Record<string, string> = {
  "cell.js":
    '/* mtoc2 cell-array runtime helpers — js-aot + interpreter side.\n *\n * Cells live as plain JS objects shaped like numbl\'s `RuntimeCell`:\n *\n *   { mtoc2Tag: "cell", shape: number[], data: RuntimeValue[] }\n *\n * `data` is column-major flat (matching tensors). `shape` is the cell\'s\n * dim list. The interpreter and js-aot backends share this shape so a\n * value materialised by one backend reads cleanly from the other.\n *\n * The c-aot backend uses per-shape generated typedefs and does NOT\n * register through this file — these helpers are js-side only.\n */\n\n// Numbl\'s scalar formatter (formatNumber), shared with disp so cell-slot\n// numbers print like `disp(x)` instead of raw String(v) full precision.\n// The build script strips this import when inlining all snippets.\n\n\n/** Construct a cell from a flat slot list (column-major) and a shape.\n *  Caller has already produced fresh slot values (deep_clone\'d where\n *  the slot was an owned alias), so we simply build the wrapper. */\nfunction mtoc2_cell_make(slots, shape) {\n  return { mtoc2Tag: "cell", shape: shape.slice(), data: slots };\n}\n\n/** Empty-double sentinel slot — what every fresh `cell(n, m)` slot\n *  starts as. Matches numbl\'s `type-constructors.ts:442-469`. */\nfunction mtoc2__empty_double_tensor() {\n  return {\n    mtoc2Tag: "tensor",\n    shape: [0, 0],\n    data: new Float64Array(0),\n  };\n}\n\n/** `cell(n)` / `cell(n, m)` / `cell(n, m, k, ...)`. The 1-arg square\n *  form has already been expanded to two dim args at lowering time so\n *  this helper is dim-symmetric.\n *\n *  Each axis is coerced to a non-negative integer (matching numbl —\n *  `Math.floor` then clamp at 0). The slot count is the dim product;\n *  each slot is the empty-double sentinel. */\nfunction mtoc2_cell_empty(dims) {\n  const shape = dims.map(d => {\n    const n = Math.floor(Number(d));\n    return n > 0 ? n : 0;\n  });\n  let total = 1;\n  for (const s of shape) total *= s;\n  const data = new Array(total);\n  for (let i = 0; i < total; i++) data[i] = mtoc2__empty_double_tensor();\n  return { mtoc2Tag: "cell", shape, data };\n}\n\n/** Format a cell value to match numbl\'s `formatCell` byte-for-byte:\n *  `{e1, e2, ...}` with chars in `\'...\'`, strings in `"..."`, other\n *  values via `formatValue`. Empty cell renders as `{}`. */\nfunction mtoc2_format_cell(c) {\n  if (c.data.length === 0) return "{}";\n  const parts = c.data.map(v => mtoc2__format_cell_slot(v));\n  return `{${parts.join(", ")}}`;\n}\n\nfunction mtoc2__format_cell_slot(v) {\n  if (v === null || typeof v !== "object") {\n    if (typeof v === "string") return `"${v}"`;\n    return mtoc2__format_scalar(v);\n  }\n  if (v.mtoc2Tag === "char") return `\'${v.value}\'`;\n  if (v.mtoc2Tag === "cell") return mtoc2_format_cell(v);\n  if (v.mtoc2Tag === "tensor") return mtoc2__format_tensor(v);\n  // Fallback: best-effort string.\n  return String(v);\n}\n\nfunction mtoc2__format_scalar(v) {\n  if (typeof v === "boolean") return v ? "1" : "0";\n  if (typeof v === "number") {\n    // Format like disp(x) / numbl\'s formatNumber (pi -> "3.1416"), not\n    // String(v)\'s full round-trip precision (-> "3.141592653589793").\n    return mtoc2_format_double(v);\n  }\n  return String(v);\n}\n\nfunction mtoc2__format_tensor(t) {\n  // Inline tensor format (no trailing newline) — matches numbl\'s\n  // `formatTensor` in a cell context. 2-D row/col-major; for cells we\n  // expect 1×N or scalar shapes commonly. Multi-row tensors render\n  // with internal `\\n` between rows.\n  if (t.data.length === 0) return "[]";\n  const rows = t.shape[0] ?? 1;\n  const cols = t.shape[1] ?? 1;\n  const cell = new Array(rows * cols);\n  let colWidths = new Array(cols).fill(0);\n  for (let c = 0; c < cols; c++) {\n    for (let r = 0; r < rows; r++) {\n      const idx = r + c * rows;\n      const s = mtoc2__format_scalar(t.data[idx]);\n      cell[idx] = s;\n      if (s.length > colWidths[c]) colWidths[c] = s.length;\n    }\n  }\n  const rowsOut = [];\n  for (let r = 0; r < rows; r++) {\n    const parts = ["   "];\n    for (let c = 0; c < cols; c++) {\n      const idx = r + c * rows;\n      const s = cell[idx];\n      parts.push(" ".repeat(colWidths[c] - s.length));\n      parts.push(s);\n      if (c < cols - 1) parts.push("   ");\n    }\n    rowsOut.push(parts.join(""));\n  }\n  return rowsOut.join("\\n");\n}\n',
  "loop_count.js":
    "// JS sibling of `loop_count.h`. Mirrors numbl's `makeRangeTensor`\n// count formula: `floor((end - start) / step + 1 + 1e-10)`, clamped\n// to non-negative.\n\nfunction mtoc2_loop_count(start, end, step) {\n  if (step === 0) return 0;\n  const n = Math.floor((end - start) / step + 1 + 1e-10);\n  if (!Number.isFinite(n)) return 0;\n  if (n <= 0) return 0;\n  return n;\n}\n",
  "range_value.js":
    "// JS sibling of `range_value.h`. Snap-to-end for the last element\n// of a `start:step:end` range so cross-runner test output matches\n// numbl byte-for-byte.\n\nfunction mtoc2_range_value(start, step, end, count, i) {\n  const v = start + step * i;\n  // Snap the last element to exactly `end` — but only for multi-element\n  // ranges, matching makeRangeTensor's `n > 1` guard. A single-element\n  // range (count === 1) is just `start`; snapping it would wrongly pull\n  // it to `end` (e.g. `0:1000:1e-9` is [0], not [1e-9]).\n  if (\n    count > 1 &&\n    i === count - 1 &&\n    Math.abs(v - end) < Math.abs(step) * 1e-10\n  ) {\n    return end;\n  }\n  return v;\n}\n",
  "scalar_index.js":
    "// JS helpers for scalar tensor indexing (1-based, MATLAB-style).\n// Mirrors the bounds-checking entry points the C side calls\n// (`mtoc2_idx_lin` / `mtoc2_idx_axis`) but returns the 0-based\n// offset directly — no out-parameter dance.\n//\n// Index values are rounded to the nearest integer (matching numbl's\n// `Math.round(idx) - 1` convention in runtime/runtimeIndexing.ts), so\n// `a(2.5)` reads element 3, not 2. Truncation would diverge from\n// numbl and produce subtly wrong answers on any non-integer index.\n\nfunction rangeError(idx, bound, where) {\n  throw new RangeError(\n    `Index in position ${where} (${idx}) exceeds array bounds (${bound})`\n  );\n}\n\n/** Linear (column-major) 0-based offset for a 1-based MATLAB index. */\nfunction mtoc2_idx_lin_js(t, k) {\n  const i = Math.round(k);\n  if (i < 1 || i > t.data.length) rangeError(i, t.data.length, 1);\n  return i - 1;\n}\n\n/** Per-axis (0-based for the axis index, 1-based for the value)\n *  bounds-checked subscript. */\nfunction mtoc2_idx_axis_js(t, axis, k) {\n  const i = Math.round(k);\n  const dim = t.shape[axis] ?? 1;\n  if (i < 1 || i > dim) rangeError(i, dim, axis + 1);\n  return i - 1;\n}\n\n// Grow-aware store subscripts (JS siblings of the C `mtoc2_idx_*_grow`\n// helpers in `grow_bail.h`). An indexed STORE whose index exceeds the\n// current extent would GROW the array in MATLAB; the JIT fixes the\n// shape at compile time and can't model the new carrier, so it throws\n// a tagged sentinel. The whole-scope / loop executor recognizes the\n// `mtoc2GrowBail` tag, bails to the interpreter (which has full grow\n// semantics), and surfaces a one-time warning. A sub-1 index is NOT a\n// grow — it's a genuine error — and still throws a plain RangeError.\n// (Statically-provable grows like `v(end+1) = x` on a fixed-shape\n// array are declined at compile time and never reach these.)\nfunction growBailError(idx, len) {\n  const e = new Error(\n    `mtoc2: indexed assignment at ${idx} would grow a ${len}-element ` +\n      `array; array growth is not supported in the JIT`\n  );\n  e.mtoc2GrowBail = true;\n  return e;\n}\n\n/** Linear (1-arg) store subscript: 0-based offset in bounds, grow-bail\n *  past the end, RangeError on sub-1. */\nfunction mtoc2_idx_lin_grow_js(t, k) {\n  const i = Math.round(k);\n  if (i < 1) rangeError(i, t.data.length, 1);\n  if (i > t.data.length) throw growBailError(i, t.data.length);\n  return i - 1;\n}\n\n/** Per-axis (N-arg) store subscript. Same contract as\n *  `mtoc2_idx_lin_grow_js` against a single axis's dim. */\nfunction mtoc2_idx_axis_grow_js(t, axis, k) {\n  const i = Math.round(k);\n  const dim = t.shape[axis] ?? 1;\n  if (i < 1) rangeError(i, dim, axis + 1);\n  if (i > dim) throw growBailError(i, dim);\n  return i - 1;\n}\n",
  "tensor_make_range.js":
    "// JS sibling of `tensor_make_range.h`. Build a 1×N row tensor for a\n// `start:step:end` range used as a value (outside an index slot and\n// outside a for-loop iterable).\n\n\n\n\nfunction mtoc2_tensor_make_range(start, step, end) {\n  const n = mtoc2_loop_count(start, end, step);\n  const t = mtoc2_tensor_alloc_nd(2, [1, n]);\n  for (let k = 0; k < n; k++) {\n    t.data[k] = mtoc2_range_value(start, step, end, n, k);\n  }\n  return t;\n}\n",
  "assert_fmt.js":
    "// JS sibling of `assert_fmt.h`. Format-string variant of `assert`.\n// Numbl-compatible: format `msg` with `args`, throw on falsy `cond`.\n\n\nfunction mtoc2_assert_scalar_fmt(cond, fmt, ...args) {\n  if (cond) return;\n  throw new Error(mtoc2_sprintf_format(fmt, args));\n}\n",
  "disp_complex.js":
    '// JS sibling of `disp_complex.h` — disp(z) for a scalar complex\n// `{re, im}`. Mirrors numbl\'s `formatComplex` via\n// `mtoc2_format_complex` and adds the trailing newline.\n\n\nfunction mtoc2_disp_complex(z) {\n  $write(mtoc2_format_complex(z) + "\\n");\n}\n',
  "disp_double.js":
    '// JS sibling of `disp_double.h` — disp(x) for a real-scalar double.\n// Mirrors numbl\'s `formatNumber` via `mtoc2_format_double` and adds\n// the trailing newline. References `$write` as a free variable\n// resolved to `globalThis.$write` at call time.\n//\n// The import keeps the source loadable as a real ES module (the\n// interpreter resolves it that way). The build script strips this\n// import when generating the inline-text version for codegen — at\n// that point all snippets are inlined into one emitted module, so\n// `mtoc2_format_double` resolves via module scope.\n\n\nfunction mtoc2_disp_double(x) {\n  $write(mtoc2_format_double(x) + "\\n");\n}\n',
  "disp_struct.js":
    '// JS sibling for struct disp. The c-aot path emits a per-typedef\n// `<typedef>_disp(v)` function via emitNamedTypedef; JS doesn\'t have\n// per-struct codegen, so we ship one generic helper that walks the\n// struct\'s keys at runtime and dispatches on the value\'s shape.\n//\n// Format mirrors numbl\'s:\n//   "    <field>: <value>\\n"  for scalar fields\n//   "    <field>: <space><cells>" for tensor fields (mtoc2_disp_tensor\n//                                  emits its own 3-space indent + \\n)\n//   "    <field>: \\n<nested>"   for nested struct fields\n// References `$write` and the disp helpers as free vars, which\n// resolve through the surrounding inlined snippet bodies / interpreter\n// imports the same way the other io helpers do.\n\n\n\nfunction mtoc2_disp_struct(v) {\n  for (const k of Object.keys(v)) {\n    const val = v[k];\n    $write(`    ${k}: `);\n    if (typeof val === "number") {\n      mtoc2_disp_double(val);\n    } else if (typeof val === "boolean") {\n      mtoc2_disp_double(val ? 1 : 0);\n    } else if (typeof val === "string") {\n      $write(val + "\\n");\n    } else if (val && val.mtoc2Tag === "tensor") {\n      mtoc2_disp_tensor(val);\n    } else if (val && val.mtoc2Tag === "char") {\n      $write(val.value + "\\n");\n    } else if (val && typeof val === "object" && "re" in val && "im" in val) {\n      // Complex scalar — print `re + imi` in a numbl-compatible form\n      // (Phase 5 may want to share this with the complex disp helper).\n      const re = val.re;\n      const im = val.im;\n      const sign = im < 0 ? " - " : " + ";\n      $write(`${re}${sign}${Math.abs(im)}i\\n`);\n    } else if (val && typeof val === "object") {\n      // Nested struct — newline then recurse. Numbl\'s format prints\n      // the parent field\'s value half as just a newline, then the\n      // nested fields render with their own indent.\n      $write("\\n");\n      mtoc2_disp_struct(val);\n    } else {\n      $write(String(val) + "\\n");\n    }\n  }\n}\n',
  "disp_tensor.js":
    '// JS sibling of `disp_tensor.h`. Real-only tensor display, mirroring\n// numbl\'s `format2DSlice` for the 2-D path and numbl\'s page-by-page\n// rendering for ndim > 2. Each cell is formatted via\n// `mtoc2_format_double`; columns are padded to their widest element;\n// rows are separated by `\\n`, columns by 3 spaces, indented by 3\n// spaces.\n\n\n// Mirrors numbl\'s `runtime/display.ts` format2DSlice: matrices\n// wider/taller than 20 are truncated to the first/last 10 with a\n// "Columns 1 through N" header and a "..." elision row/column. Without\n// this the JIT printed every element on one line, diverging from the\n// interpreter.\nconst MTOC2_MAX_DISPLAY_ROWS = 20;\nconst MTOC2_MAX_DISPLAY_COLS = 20;\n\nfunction disp_real_slice(data, offset, rows, cols) {\n  const truncRows = rows > MTOC2_MAX_DISPLAY_ROWS;\n  const truncCols = cols > MTOC2_MAX_DISPLAY_COLS;\n  const rHi = Math.ceil(MTOC2_MAX_DISPLAY_ROWS / 2);\n  const rLo = Math.floor(MTOC2_MAX_DISPLAY_ROWS / 2);\n  const cHi = Math.ceil(MTOC2_MAX_DISPLAY_COLS / 2);\n  const cLo = Math.floor(MTOC2_MAX_DISPLAY_COLS / 2);\n  const showRows = [];\n  if (truncRows) {\n    for (let i = 0; i < rHi; i++) showRows.push(i);\n    for (let i = 0; i < rLo; i++) showRows.push(rows - rLo + i);\n  } else {\n    for (let i = 0; i < rows; i++) showRows.push(i);\n  }\n  const showCols = [];\n  if (truncCols) {\n    for (let i = 0; i < cHi; i++) showCols.push(i);\n    for (let i = 0; i < cLo; i++) showCols.push(cols - cLo + i);\n  } else {\n    for (let i = 0; i < cols; i++) showCols.push(i);\n  }\n\n  if (truncRows || truncCols) {\n    $write("  Columns 1 through " + cols + "\\n");\n    $write("\\n");\n  }\n\n  const formatted = [];\n  const colWidths = new Array(showCols.length + (truncCols ? 1 : 0)).fill(0);\n  for (const r of showRows) {\n    const row = [];\n    let ci = 0;\n    for (const c of showCols) {\n      const text = mtoc2_format_double(data[offset + r + c * rows]);\n      row.push(text);\n      if (text.length > colWidths[ci]) colWidths[ci] = text.length;\n      ci++;\n      if (truncCols && ci === cHi) {\n        row.push("...");\n        if (colWidths[ci] < 3) colWidths[ci] = 3;\n        ci++;\n      }\n    }\n    formatted.push(row);\n  }\n\n  let fi = 0;\n  for (let si = 0; si < showRows.length; si++) {\n    if (truncRows && si === rHi) {\n      $write("   " + colWidths.map(w => "...".padStart(w)).join("   ") + "\\n");\n    }\n    const parts = formatted[fi].map((s, c) => s.padStart(colWidths[c]));\n    $write("   " + parts.join("   ") + "\\n");\n    fi++;\n  }\n}\n\nfunction mtoc2_disp_tensor(t) {\n  // Mirrors numbl: empty tensors print nothing.\n  if (!t || !t.shape || t.shape.length === 0) return;\n  const rows = t.shape[0] ?? 1;\n  const cols = t.shape[1] ?? 1;\n  let total = 1;\n  for (const s of t.shape) total *= s;\n  if (total <= 0) return;\n  // 1-element tensors print as bare scalars (no column-aligned\n  // indent) — matches numbl\'s `runtime/display.ts:128` special case.\n  if (total === 1) {\n    $write(mtoc2_format_double(t.data[0]) + "\\n");\n    return;\n  }\n\n  const pageSize = rows * cols;\n  let numPages = 1;\n  for (let i = 2; i < t.shape.length; i++) numPages *= t.shape[i];\n\n  for (let p = 0; p < numPages; p++) {\n    if (t.shape.length > 2) {\n      if (p > 0) $write("\\n");\n      let rem = p;\n      let header = "(:,:";\n      for (let i = 2; i < t.shape.length; i++) {\n        const d = t.shape[i];\n        const s = rem % d;\n        rem = Math.floor(rem / d);\n        header += "," + (s + 1);\n      }\n      header += ") =\\n\\n";\n      $write(header);\n    }\n    disp_real_slice(t.data, p * pageSize, rows, cols);\n  }\n}\n',
  "disp_tensor_complex.js":
    '// JS sibling of `disp_tensor_complex.h`. Display a complex tensor\n// with each cell formatted via `mtoc2_format_complex` and columns\n// padded to their widest cell.\n\n\nfunction disp_complex_slice(re, im, offset, rows, cols) {\n  const cells = new Array(rows * cols);\n  const colWidths = new Array(cols).fill(0);\n  for (let c = 0; c < cols; c++) {\n    for (let r = 0; r < rows; r++) {\n      const idx = r + c * rows;\n      const text = mtoc2_format_complex({\n        re: re[offset + idx],\n        im: im[offset + idx],\n      });\n      cells[idx] = text;\n      if (text.length > colWidths[c]) colWidths[c] = text.length;\n    }\n  }\n  for (let r = 0; r < rows; r++) {\n    let line = "   ";\n    for (let c = 0; c < cols; c++) {\n      const idx = r + c * rows;\n      const cell = cells[idx];\n      const pad = colWidths[c] - cell.length;\n      for (let i = 0; i < pad; i++) line += " ";\n      line += cell;\n      if (c < cols - 1) line += "   ";\n    }\n    line += "\\n";\n    $write(line);\n  }\n}\n\nfunction mtoc2_disp_tensor_complex(t) {\n  if (!t || !t.shape || t.shape.length === 0) return;\n  if (t.imag === undefined) return; // shouldn\'t happen for a complex tensor\n  const rows = t.shape[0] ?? 1;\n  const cols = t.shape[1] ?? 1;\n  let total = 1;\n  for (const s of t.shape) total *= s;\n  if (total <= 0) return;\n\n  const pageSize = rows * cols;\n  let numPages = 1;\n  for (let i = 2; i < t.shape.length; i++) numPages *= t.shape[i];\n\n  for (let p = 0; p < numPages; p++) {\n    if (t.shape.length > 2) {\n      if (p > 0) $write("\\n");\n      let rem = p;\n      let header = "(:,:";\n      for (let i = 2; i < t.shape.length; i++) {\n        const d = t.shape[i];\n        const s = rem % d;\n        rem = Math.floor(rem / d);\n        header += "," + (s + 1);\n      }\n      header += ") =\\n\\n";\n      $write(header);\n    }\n    disp_complex_slice(t.data, t.imag, p * pageSize, rows, cols);\n  }\n}\n',
  "error_fmt.js":
    "// JS sibling of `error_fmt.h`. Format the message + arguments and\n// throw a runtime error. Mirrors numbl's `error(...)` behavior\n// (stderr + halt).\n//\n// The thrown error carries an `identifier` string property so a\n// `try / catch ME` arm in the interpreter can populate the `ME`\n// struct's `identifier` field. When the caller passes no identifier\n// (the common `error('msg')` form), it's the empty string.\n\n\nfunction mtoc2_error_fmt(fmt, ...args) {\n  const msg = mtoc2_sprintf_format(fmt, args);\n  // Numbl writes to stderr; in the JS host we route through the same\n  // `$write` channel by emitting a \"stderr\" sentinel prefix the host\n  // can split on. For now, throw — the CLI's runner formats the\n  // exception to stderr, matching the user-visible result.\n  const err = new Error(msg);\n  err.identifier = \"\";\n  throw err;\n}\n\nfunction mtoc2_error_fmt_id(identifier, fmt, ...args) {\n  const msg = mtoc2_sprintf_format(fmt, args);\n  const err = new Error(msg);\n  err.identifier = identifier;\n  throw err;\n}\n",
  "format_complex.js":
    '// JS sibling of `format_complex.h`. Format a scalar complex `{re, im}`\n// the way numbl\'s `formatComplex` does. Each component is rendered\n// through `mtoc2_format_double` so the scalar formatting stays in\n// lockstep with numbl\'s `formatNumber`.\n\n\nfunction mtoc2_format_complex(z) {\n  const re = z.re;\n  const im = z.im;\n  if (im === 0) return mtoc2_format_double(re);\n  if (re === 0) return mtoc2_format_double(im) + "i";\n  if (im < 0) {\n    return mtoc2_format_double(re) + " - " + mtoc2_format_double(-im) + "i";\n  }\n  return mtoc2_format_double(re) + " + " + mtoc2_format_double(im) + "i";\n}\n',
  "format_double.js":
    '// JS sibling of `format_double.h` — same numbl-compatible formatter,\n// in JS. Used by `emitJs` for the `disp` scalar path and (later) by\n// the interpreter via the re-export in `snippets.gen.ts`.\n\nfunction mtoc2_format_double(x) {\n  if (Number.isNaN(x)) return "NaN";\n  // MATLAB spells these Inf / -Inf (matches the interpreter\'s formatNumber).\n  if (x === Infinity) return "Inf";\n  if (x === -Infinity) return "-Inf";\n  if (x === 0) x = 0;\n  if (Math.abs(x) < 1e15 && x === Math.trunc(x)) return String(x);\n\n  const buf = x.toExponential(4);\n  const ePos = buf.indexOf("e");\n  if (ePos < 0) return buf;\n  const decExp = parseInt(buf.slice(ePos + 1), 10);\n\n  if (decExp < -6 || decExp >= 5) {\n    let mantissaEnd = ePos - 1;\n    while (mantissaEnd > 0 && buf[mantissaEnd] === "0") mantissaEnd--;\n    if (mantissaEnd >= 0 && buf[mantissaEnd] === ".") mantissaEnd--;\n    let expPart = buf.slice(ePos + 1);\n    let sign = "+";\n    if (expPart[0] === "+" || expPart[0] === "-") {\n      sign = expPart[0];\n      expPart = expPart.slice(1);\n    }\n    while (expPart.length > 1 && expPart[0] === "0") expPart = expPart.slice(1);\n    return buf.slice(0, mantissaEnd + 1) + "e" + sign + expPart;\n  }\n\n  let fracDigits = 4 - decExp;\n  if (fracDigits < 0) fracDigits = 0;\n  let s = x.toFixed(fracDigits);\n  if (s.includes(".")) {\n    while (s.endsWith("0")) s = s.slice(0, -1);\n    if (s.endsWith(".")) s = s.slice(0, -1);\n  }\n  return s;\n}\n',
  "format_engine.js":
    '// JS sibling of `format_engine.h`. Numbl-compatible printf-style\n// formatter, ported from `numbl/src/numbl-core/helpers/string.ts`\'s\n// `sprintfFormat`. Shared by fprintf / sprintf / error / assert on\n// the JS side.\n//\n// Spec set: d i u f e E g G x X o s c %\n// Flags: - + 0 # space. Precision `.N`. Width digits. `*` consumes the\n// next numeric arg as a width.\n// Escapes interpreted AT FORMAT TIME: \\n \\t \\\\.\n// Numeric tensors flatten column-major into the scalar stream; the\n// format string cycles through args until consumed (with a "no arg\n// consumed this pass" guard against infinite output).\n\nfunction isTensor(v) {\n  return typeof v === "object" && v !== null && v.mtoc2Tag === "tensor";\n}\nfunction isChar(v) {\n  return typeof v === "object" && v !== null && v.mtoc2Tag === "char";\n}\n\nfunction toNumber(v) {\n  if (typeof v === "number") return v;\n  if (typeof v === "boolean") return v ? 1 : 0;\n  if (typeof v === "string") return Number(v);\n  if (isChar(v)) {\n    // Match the interpreter\'s toNumber (runtime/convert.ts): a single\n    // char yields its code point; a multi-char value has no scalar\n    // numeric form. `Number("A")` would wrongly give NaN.\n    if (v.value.length === 1) return v.value.charCodeAt(0);\n    throw new Error("Cannot convert multi-char to number");\n  }\n  if (isTensor(v) && v.data.length === 1) return v.data[0];\n  return Number(v);\n}\n\nfunction toString(v) {\n  if (typeof v === "string") return v;\n  if (typeof v === "number") return String(v);\n  if (typeof v === "boolean") return v ? "1" : "0";\n  if (isChar(v)) return v.value;\n  return String(v);\n}\n\nfunction numStr(n) {\n  if (n === Infinity) return "Inf";\n  if (n === -Infinity) return "-Inf";\n  if (Number.isNaN(n)) return "NaN";\n  if (n === 0) return "0";\n  const prec = 5;\n  const exp = Math.floor(Math.log10(Math.abs(n)));\n  let s;\n  if (exp < -4 || exp >= prec) {\n    s = n.toExponential(prec - 1);\n    const ePos = s.indexOf("e");\n    let mantissa = s.slice(0, ePos);\n    const expPart0 = s.slice(ePos);\n    if (mantissa.includes(".")) mantissa = mantissa.replace(/\\.?0+$/, "");\n    const expPart = expPart0.replace(/([eE][+-])(\\d)$/, "$1" + "0$2");\n    s = mantissa + expPart;\n  } else {\n    if (Number.isInteger(n)) return String(n);\n    s = n.toPrecision(prec);\n    if (s.includes(".")) s = s.replace(/\\.?0+$/, "");\n  }\n  return s;\n}\n\nfunction applyWidth(spec, str) {\n  const m = spec.match(/^%([-+ #]*)0?(\\d+)?/);\n  if (!m) return str;\n  const explicitFlags = m[1] || "";\n  const leftAlign = explicitFlags.includes("-");\n  const afterPercent = spec.slice(1);\n  const flagAndWidth = afterPercent.match(/^([-+ #]*)(0?)(\\d+)?/);\n  const zeroFlag = flagAndWidth ? flagAndWidth[2] === "0" : false;\n  const width = flagAndWidth && flagAndWidth[3] ? parseInt(flagAndWidth[3]) : 0;\n  if (width <= str.length) return str;\n  const zeroPad = !leftAlign && zeroFlag;\n  const padLen = width - str.length;\n  if (leftAlign) return str + " ".repeat(padLen);\n  if (zeroPad) {\n    if (str[0] === "-" || str[0] === "+") {\n      return str[0] + "0".repeat(padLen) + str.slice(1);\n    }\n    return "0".repeat(padLen) + str;\n  }\n  return " ".repeat(padLen) + str;\n}\n\nfunction mtoc2_sprintf_format(fmt, args) {\n  // Char literals (`\'foo\'`) come through wrapped as\n  // `{mtoc2Tag:"char", value:"..."}` to match the interpreter\'s\n  // RuntimeValue convention; unwrap so the format walker indexes\n  // a plain JS string.\n  if (isChar(fmt)) fmt = fmt.value;\n  // Flatten args column-major (tensors expand element-by-element).\n  const flat = [];\n  for (const arg of args) {\n    if (isTensor(arg)) {\n      for (let k = 0; k < arg.data.length; k++) flat.push(arg.data[k]);\n    } else {\n      flat.push(arg);\n    }\n  }\n\n  let result = "";\n  let argIdx = 0;\n\n  do {\n    const startArgIdx = argIdx;\n    let outOfArgs = false;\n    let i = 0;\n    while (i < fmt.length && !outOfArgs) {\n      if (fmt[i] === "%" && i + 1 < fmt.length) {\n        i++;\n        let spec = "%";\n        while (i < fmt.length && !"dfigeEsoxXuc%".includes(fmt[i])) {\n          if (fmt[i] === "*") {\n            if (argIdx >= flat.length) {\n              outOfArgs = true;\n              break;\n            }\n            spec += String(Math.round(toNumber(flat[argIdx++])));\n            i++;\n          } else {\n            spec += fmt[i];\n            i++;\n          }\n        }\n        if (outOfArgs) break;\n        if (i < fmt.length) {\n          const ch = fmt[i];\n          i++;\n          if (ch === "%") {\n            result += "%";\n          } else if (argIdx >= flat.length) {\n            outOfArgs = true;\n          } else if (ch === "d" || ch === "i" || ch === "u") {\n            const raw = toNumber(flat[argIdx++]);\n            const isInt = Number.isInteger(raw);\n            const canPrintAsInt = ch === "u" ? isInt && raw >= 0 : isInt;\n            if (!canPrintAsInt) {\n              let eStr = raw.toExponential(6);\n              eStr = eStr.replace(/e([+-])(\\d)$/, "e$1" + "0$2");\n              result += applyWidth(spec, eStr);\n            } else {\n              const n = raw;\n              const flags = spec.slice(1);\n              const hasPlus = flags.includes("+");\n              const leftAlign = flags.includes("-");\n              const widthMatch = spec.match(/^%[^0-9]*(\\d+)/);\n              const width = widthMatch ? parseInt(widthMatch[1]) : 0;\n              const zeroPad = !leftAlign && /^[-+ ]*0/.test(spec.slice(1));\n              const s = String(Math.abs(n));\n              const sign = n < 0 ? "-" : hasPlus ? "+" : "";\n              if (width > 0) {\n                const padChar = zeroPad ? "0" : " ";\n                const padLen = Math.max(0, width - sign.length - s.length);\n                const pad = padChar.repeat(padLen);\n                result += leftAlign\n                  ? sign + s + " ".repeat(padLen)\n                  : zeroPad\n                    ? sign + pad + s\n                    : pad + sign + s;\n              } else {\n                result += sign + s;\n              }\n            }\n          } else if (ch === "f") {\n            const n = toNumber(flat[argIdx++]);\n            if (!isFinite(n) || isNaN(n)) {\n              result += applyWidth(spec, numStr(n));\n            } else {\n              const fFlags = spec.slice(1);\n              const fHasPlus = fFlags.includes("+");\n              const precMatch = spec.match(/\\.(\\d+)/);\n              const prec = precMatch ? parseInt(precMatch[1]) : 6;\n              const formatted = n.toFixed(prec);\n              const fSign = n < 0 ? "" : fHasPlus ? "+" : "";\n              result += applyWidth(spec, fSign + formatted);\n            }\n          } else if (ch === "e" || ch === "E") {\n            const n = toNumber(flat[argIdx++]);\n            if (!isFinite(n) || isNaN(n)) {\n              result += applyWidth(spec, numStr(n));\n            } else {\n              const precMatch = spec.match(/\\.(\\d+)/);\n              const prec = precMatch ? parseInt(precMatch[1]) : 6;\n              let eStr = n.toExponential(prec);\n              eStr = eStr.replace(/e([+-])(\\d)$/, "e$1" + "0$2");\n              if (ch === "E") eStr = eStr.toUpperCase();\n              result += applyWidth(spec, eStr);\n            }\n          } else if (ch === "x" || ch === "X") {\n            const n = Math.round(toNumber(flat[argIdx++]));\n            let s = Math.abs(n).toString(16);\n            if (ch === "X") s = s.toUpperCase();\n            result += applyWidth(spec, s);\n          } else if (ch === "o") {\n            const n = Math.round(toNumber(flat[argIdx++]));\n            result += applyWidth(spec, Math.abs(n).toString(8));\n          } else if (ch === "g" || ch === "G") {\n            const gVal = toNumber(flat[argIdx++]);\n            if (!isFinite(gVal) || isNaN(gVal)) {\n              result += applyWidth(spec, numStr(gVal));\n            } else {\n              const precMatch = spec.match(/\\.(\\d+)/);\n              const gPrec = precMatch ? parseInt(precMatch[1]) : 6;\n              let gStr;\n              if (gVal === 0) {\n                gStr = "0";\n              } else {\n                const exp = Math.floor(Math.log10(Math.abs(gVal)));\n                if (exp < -4 || exp >= gPrec) {\n                  gStr = gVal.toExponential(gPrec - 1);\n                  const ePos = gStr.indexOf("e");\n                  let mantissa = gStr.slice(0, ePos);\n                  let expPart = gStr.slice(ePos);\n                  if (mantissa.includes(".")) {\n                    mantissa = mantissa.replace(/\\.?0+$/, "");\n                  }\n                  expPart = expPart.replace(/e([+-])(\\d)$/, "e$1" + "0$2");\n                  gStr = mantissa + expPart;\n                } else {\n                  gStr = gVal.toPrecision(gPrec);\n                  if (gStr.includes(".")) {\n                    gStr = gStr.replace(/\\.?0+$/, "");\n                  }\n                  if (gStr.includes("e")) {\n                    gStr = String(parseFloat(gStr));\n                  }\n                }\n              }\n              if (ch === "G") gStr = gStr.toUpperCase();\n              result += applyWidth(spec, gStr);\n            }\n          } else if (ch === "s") {\n            const sArg = flat[argIdx++];\n            // A numeric/logical argument to %s is a character code (MATLAB:\n            // sprintf(\'%s\', 65) -> \'A\').\n            const sVal =\n              typeof sArg === "number" || typeof sArg === "boolean"\n                ? String.fromCharCode(Math.round(toNumber(sArg)))\n                : toString(sArg);\n            const sFlags = spec.slice(1);\n            const sLeftAlign = sFlags.includes("-");\n            const sWidthMatch = spec.match(/^%[^0-9]*(\\d+)/);\n            const sWidth = sWidthMatch ? parseInt(sWidthMatch[1]) : 0;\n            if (sWidth > sVal.length) {\n              const sPad = " ".repeat(sWidth - sVal.length);\n              result += sLeftAlign ? sVal + sPad : sPad + sVal;\n            } else {\n              result += sVal;\n            }\n          } else if (ch === "c") {\n            result += String.fromCharCode(Math.round(toNumber(flat[argIdx++])));\n          } else {\n            result += spec + ch;\n            argIdx++;\n          }\n        }\n      } else if (fmt[i] === "\\\\" && i + 1 < fmt.length) {\n        i++;\n        switch (fmt[i]) {\n          case "n":\n            result += "\\n";\n            break;\n          case "t":\n            result += "\\t";\n            break;\n          case "\\\\":\n            result += "\\\\";\n            break;\n          default:\n            result += "\\\\" + fmt[i];\n        }\n        i++;\n      } else {\n        result += fmt[i];\n        i++;\n      }\n    }\n    if (argIdx === startArgIdx) break;\n  } while (argIdx < flat.length);\n\n  return result;\n}\n',
  "fprintf.js":
    "// JS sibling of `fprintf.h`. Routes formatted output through\n// `mtoc2_sprintf_format` then writes via `$write` (the JS analog of\n// C's stdout). Numbl-compatible spec set & semantics.\n\n\nfunction mtoc2_fprintf(fmt, ...args) {\n  $write(mtoc2_sprintf_format(fmt, args));\n}\n",
  "sprintf.js":
    '// JS sibling of `sprintf.h`. Two output variants:\n//   - `mtoc2_sprintf_str(fmt, args)` returns the formatted text as a\n//     plain JS string (the equivalent of numbl\'s `String` kind).\n//   - `mtoc2_sprintf_char(fmt, args)` returns it as a `mtoc2_char_tensor_t`\n//     placeholder — we keep the JS shape as a `{mtoc2Tag: "char", value}`\n//     object so disp/length etc. dispatch correctly.\n\n\nfunction mtoc2_sprintf_str(fmt, ...args) {\n  return mtoc2_sprintf_format(fmt, args);\n}\n\nfunction mtoc2_sprintf_char(fmt, ...args) {\n  return { mtoc2Tag: "char", value: mtoc2_sprintf_format(fmt, args) };\n}\n',
  "warning_fmt.js":
    '// JS sibling of `warning_fmt.h`. Format the message and write\n// `Warning: <msg>\\n` via `$write`. Matches numbl\n// (`runtime/specialBuiltins.ts:190` — `rt.output("Warning: " + msg + "\\n")`).\n\n\nfunction mtoc2_warning_fmt(fmt, ...args) {\n  $write("Warning: " + mtoc2_sprintf_format(fmt, args) + "\\n");\n}\n\nfunction mtoc2_warning_fmt_id(_id, fmt, ...args) {\n  // The id is informational — numbl threads it to lastwarn state,\n  // which mtoc2 doesn\'t expose. Drop it here so the visible output\n  // matches the bare form.\n  $write("Warning: " + mtoc2_sprintf_format(fmt, args) + "\\n");\n}\n',
  "plot_dispatch.js":
    '// JS sibling of `plot_dispatch.h`. Two delivery modes:\n//\n// 1. Host hook mode (numbl JIT bridge): when the host binds a\n//    `$plotDispatch(name, args)` callback into the emit\'s `$h`\n//    helpers, the helper forwards the raw mtoc2-shaped args to the\n//    host. The host is expected to translate the args into its own\n//    runtime value shape and route them into its plot pipeline\n//    (numbl: `dispatchPlotBuiltin` into `rt.plotInstructions`).\n//\n//    Wire-format encoding is skipped entirely in this mode — the\n//    host operates in-process and consumes the same value graph the\n//    emitted code already holds.\n//\n// 2. Wire-format mode (standalone AOT / viewer tee): when no host\n//    hook is present, fall back to the RS-prefixed JSON record on\n//    stdout that the launcher splits and forwards. Same shape the C\n//    sibling emits, so a single viewer parses both backends.\n//\n// Wire shape per call (mode 2):\n//   \\x1e mtoc2:plot \\t {"call":"<name>","args":[<arg>, ...]} \\n\n//\n// Arg encoding (per source-level arg):\n//   number → bare numeric (non-finite → null, matching the C side\n//            and JSON.stringify\'s natural behavior)\n//   string → {"kind":"text","data":"<text>"}\n//   char   → same as string (MATLAB plot semantics don\'t distinguish)\n//   tensor → {"kind":"tensor","dims":[…],"data":[…]} (column-major\n//            flatten; non-finite as null)\n\nfunction isTensor(v) {\n  return typeof v === "object" && v !== null && v.mtoc2Tag === "tensor";\n}\nfunction isChar(v) {\n  return typeof v === "object" && v !== null && v.mtoc2Tag === "char";\n}\n\nfunction encodeArg(v) {\n  if (typeof v === "number") {\n    return Number.isFinite(v) ? v : null;\n  }\n  if (typeof v === "boolean") return v ? 1 : 0;\n  if (typeof v === "string") return { kind: "text", data: v };\n  if (isChar(v)) return { kind: "text", data: v.value };\n  if (isTensor(v)) {\n    const data = new Array(v.data.length);\n    for (let i = 0; i < v.data.length; i++) {\n      const x = v.data[i];\n      data[i] = Number.isFinite(x) ? x : null;\n    }\n    return { kind: "tensor", dims: v.shape.slice(), data };\n  }\n  // Unknown shape — pass through. Type-level rejection on the\n  // lowering side keeps complex/struct/class/handle out of here.\n  return String(v);\n}\n\nfunction mtoc2_plot_dispatch(name, ...args) {\n  if (typeof globalThis.$plotDispatch === "function") {\n    globalThis.$plotDispatch(name, args);\n    return;\n  }\n  const record = { call: name, args: args.map(encodeArg) };\n  $write("\\x1emtoc2:plot\\t" + JSON.stringify(record) + "\\n");\n}\n',
  "cdiv.js":
    "// JS sibling of `cdiv.h`. Scalar complex division matching numbl's\n// signed-Inf-on-zero-divisor behavior via Smith's algorithm. Mirrors\n// the C side so cross-runner output stays byte-for-byte.\n\nfunction mtoc2_cdiv(a, b) {\n  const ar = a.re,\n    ai = a.im;\n  const br = b.re,\n    bi = b.im;\n  // Zero divisor: match the interpreter (helpers/arithmetic.ts) — 0/0 is\n  // NaN, a nonzero numerator yields a signed Inf per component. Smith's\n  // algorithm below would otherwise produce NaN+NaNi here.\n  if (br === 0 && bi === 0) {\n    if (ar === 0 && ai === 0) return { re: NaN, im: 0 };\n    const signedInf = x => (x > 0 ? Infinity : x < 0 ? -Infinity : 0);\n    return { re: signedInf(ar), im: signedInf(ai) };\n  }\n  if (Math.abs(br) >= Math.abs(bi)) {\n    const r = bi / br;\n    const den = br + r * bi;\n    return { re: (ar + ai * r) / den, im: (ai - ar * r) / den };\n  }\n  const r = br / bi;\n  const den = bi + r * br;\n  return { re: (ar * r + ai) / den, im: (ai * r - ar) / den };\n}\n",
  "cscalar.js":
    "// JS sibling of `cscalar.h`. JS has no native complex type, so each\n// helper takes and returns `{re, im}` objects. Mirrors the C\n// signatures so emitJs renders calls structurally identical to the\n// C path (just with JS objects instead of `double _Complex`).\n//\n// Division (`mtoc2_cdiv`) lives in `cdiv.js` because it needs\n// Smith's algorithm with signed-zero detection to match numbl\n// byte-for-byte.\n\nfunction mtoc2_cmake(re, im) {\n  return { re, im };\n}\nfunction mtoc2_creal(z) {\n  return z.re;\n}\nfunction mtoc2_cimag(z) {\n  return z.im;\n}\nfunction mtoc2_cadd(a, b) {\n  return { re: a.re + b.re, im: a.im + b.im };\n}\nfunction mtoc2_csub(a, b) {\n  return { re: a.re - b.re, im: a.im - b.im };\n}\nfunction mtoc2_cmul(a, b) {\n  return { re: a.re * b.re - a.im * b.im, im: a.re * b.im + a.im * b.re };\n}\nfunction mtoc2_cneg(z) {\n  return { re: -z.re, im: -z.im };\n}\nfunction mtoc2_cconj(z) {\n  return { re: z.re, im: -z.im };\n}\nfunction mtoc2_cabs(z) {\n  return Math.hypot(z.re, z.im);\n}\nfunction mtoc2_cangle(z) {\n  return Math.atan2(z.im, z.re);\n}\nfunction mtoc2_cnonzero(z) {\n  return z.re !== 0 || z.im !== 0;\n}\nfunction mtoc2_ceq(a, b) {\n  return a.re === b.re && a.im === b.im;\n}\nfunction mtoc2_cne(a, b) {\n  return a.re !== b.re || a.im !== b.im;\n}\n\n// `cpow(a, b) = exp(b * log(a))`. Matches numbl / C99 `cpow` for\n// finite inputs; special zero / pole cases follow the same path\n// the C side picks (exp(NaN) = NaN, etc.).\nfunction clog(z) {\n  // Normalize a -0 real part to +0 so log(-0) (a real -0) gives\n  // -Inf + 0i, not -Inf + πi — MATLAB / C99 treat -0 as 0, not as an\n  // approach-from-below negative. atan2's -0 second arg only changes the\n  // result when the first arg is also 0, so this affects nothing else.\n  const reAngle = z.re === 0 ? 0 : z.re;\n  return {\n    re: Math.log(Math.hypot(z.re, z.im)),\n    im: Math.atan2(z.im, reAngle),\n  };\n}\nfunction cexp(z) {\n  const m = Math.exp(z.re);\n  // Real-valued input (im === 0): the imaginary lane is m*sin(0) = 0, but\n  // when m overflows to Inf that becomes Inf*0 = NaN. Return a clean 0\n  // (matches the interpreter's real exp and C99 cexp).\n  if (z.im === 0) return { re: m, im: 0 };\n  return { re: m * Math.cos(z.im), im: m * Math.sin(z.im) };\n}\nfunction mtoc2_cpow(a, b) {\n  // Zero base: match the interpreter's complexPow (helpers/arithmetic.ts)\n  // — 0^0 = 1, a positive real part of the exponent gives 0 (regardless\n  // of the imaginary part), and anything else gives Inf+0i. The old\n  // guard required b.im===0 for the zero case and otherwise returned\n  // NaN+NaNi.\n  if (a.re === 0 && a.im === 0) {\n    if (b.re === 0 && b.im === 0) return { re: 1, im: 0 };\n    if (b.re > 0) return { re: 0, im: 0 };\n    return { re: Infinity, im: 0 };\n  }\n  return cexp(mtoc2_cmul(b, clog(a)));\n}\n\n// Unary math.\nfunction mtoc2_csqrt(z) {\n  // Pure-real input: compute directly. Smith's formula below suffers\n  // catastrophic cancellation/overflow when z.im === 0 — e.g.\n  // sqrt(-Inf) → sqrt((Inf + -Inf)/2) = sqrt(NaN) = NaN, and\n  // sqrt(-1e308) → sqrt((1e308 - -1e308)/2) overflows to Inf instead of\n  // 1e154. Mirror the interpreter's complexSqrt (math.ts) and C's libm\n  // csqrt, both of which special-case the real axis.\n  if (z.im === 0) {\n    if (z.re >= 0) return { re: Math.sqrt(z.re), im: 0 };\n    return { re: 0, im: Math.sqrt(-z.re) };\n  }\n  // Smith's stable formula for hypot * sign.\n  const r = Math.hypot(z.re, z.im);\n  if (r === 0) return { re: 0, im: 0 };\n  const re = Math.sqrt((r + z.re) / 2);\n  const im = (z.im >= 0 ? 1 : -1) * Math.sqrt((r - z.re) / 2);\n  return { re, im };\n}\nfunction mtoc2_cexp(z) {\n  return cexp(z);\n}\nfunction mtoc2_clog(z) {\n  return clog(z);\n}\nfunction mtoc2_clog2(z) {\n  const l = clog(z);\n  const lg2 = Math.log(2);\n  return { re: l.re / lg2, im: l.im / lg2 };\n}\nfunction mtoc2_clog10(z) {\n  const l = clog(z);\n  const lg10 = Math.log(10);\n  return { re: l.re / lg10, im: l.im / lg10 };\n}\nfunction mtoc2_csin(z) {\n  // Guard 0*Inf=NaN on the imaginary axis: for a pure-imaginary input\n  // (sin(z.re)===0), the real lane is 0 and the imaginary lane is\n  // ±sinh(im); cosh/sinh overflow to Inf for large |im|, and 0*Inf would\n  // poison the result. C99 csin special-cases this — match it.\n  const sr = Math.sin(z.re);\n  const cr = Math.cos(z.re);\n  return {\n    re: sr === 0 ? 0 : sr * Math.cosh(z.im),\n    im: cr === 0 ? 0 : cr * Math.sinh(z.im),\n  };\n}\nfunction mtoc2_ccos(z) {\n  const cr = Math.cos(z.re);\n  const sr = Math.sin(z.re);\n  return {\n    re: cr === 0 ? 0 : cr * Math.cosh(z.im),\n    im: sr === 0 ? 0 : -sr * Math.sinh(z.im),\n  };\n}\nfunction mtoc2_ctan(z) {\n  // Double-angle form, matching the interpreter (interpreter/builtins/\n  // math.ts) and C99 ctan. Computing sin(z)/cos(z) with a |cos|^2\n  // denominator loses the (tiny but nonzero) real part and overflows\n  // for large imaginary parts — e.g. tan(1+200i) collapsed to 1i.\n  const denom = Math.cos(2 * z.re) + Math.cosh(2 * z.im);\n  return { re: Math.sin(2 * z.re) / denom, im: Math.sinh(2 * z.im) / denom };\n}\nfunction mtoc2_catan(z) {\n  // Real-valued input (im === 0): atan is a clean real function. The\n  // complex formula leaves a spurious ±1e-17i residue (and NaN+NaNi once\n  // 1±iz overflows, e.g. atan(1e300)). Take the real path.\n  if (z.im === 0) return { re: Math.atan(z.re), im: 0 };\n  // atan(z) = (i/2)·log((1 − iz)/(1 + iz)) — MATLAB's branch (atan(2i) =\n  // −1.5708 + 0.5493i). The C runtime overrides its libc catan (which\n  // uses the opposite +π/2 Annex-G branch) to match this.\n  const iz = { re: -z.im, im: z.re }; // i*z\n  const num = { re: 1 - iz.re, im: -iz.im }; // 1 - iz\n  const denom = { re: 1 + iz.re, im: iz.im }; // 1 + iz\n  const dd = denom.re * denom.re + denom.im * denom.im;\n  const q = {\n    re: (num.re * denom.re + num.im * denom.im) / dd,\n    im: (num.im * denom.re - num.re * denom.im) / dd,\n  };\n  const l = clog(q);\n  return { re: -l.im / 2, im: l.re / 2 };\n}\n// Hyperbolic sinh/cosh/tanh — byte-for-byte with the interpreter's\n// complex formulas (interpreter/builtins/math.ts). Real-valued input\n// (im === 0) falls out cleanly: cos(0)=1, sin(0)=0, so the imaginary\n// lane is exactly 0.\nfunction mtoc2_csinh(z) {\n  return {\n    re: Math.sinh(z.re) * Math.cos(z.im),\n    im: Math.cosh(z.re) * Math.sin(z.im),\n  };\n}\nfunction mtoc2_ccosh(z) {\n  return {\n    re: Math.cosh(z.re) * Math.cos(z.im),\n    im: Math.sinh(z.re) * Math.sin(z.im),\n  };\n}\nfunction mtoc2_ctanh(z) {\n  const denom = Math.cosh(2 * z.re) + Math.cos(2 * z.im);\n  return { re: Math.sinh(2 * z.re) / denom, im: Math.sin(2 * z.im) / denom };\n}\nfunction mtoc2_cfloor(z) {\n  return { re: Math.floor(z.re), im: Math.floor(z.im) };\n}\nfunction mtoc2_cceil(z) {\n  return { re: Math.ceil(z.re), im: Math.ceil(z.im) };\n}\nfunction mtoc2_cround(z) {\n  // MATLAB rounds each component half-away-from-zero.\n  const half = x => Math.sign(x) * Math.round(Math.abs(x));\n  return { re: half(z.re), im: half(z.im) };\n}\nfunction mtoc2_cfix(z) {\n  return { re: Math.trunc(z.re), im: Math.trunc(z.im) };\n}\nfunction mtoc2_csign(z) {\n  if (z.re === 0 && z.im === 0) return { re: 0, im: 0 };\n  const m = Math.hypot(z.re, z.im);\n  return { re: z.re / m, im: z.im / m };\n}\n",
  "deep_clone.js":
    '/* Deep-clone an owned-typed runtime value so a subsequent member /\n * indexed write through one alias doesn\'t leak to others. Mirrors\n * the c-aot per-typedef `_copy` protocol: every Var-RHS Assign /\n * MemberLoad-RHS Assign / function-call arg of an owned type is\n * wrapped through this helper at emit time, so JS-side aliasing\n * stops at the assignment boundary.\n *\n * Recognises the runtime shapes the rest of mtoc2 emits:\n *   - RuntimeTensor (`{mtoc2Tag:"tensor", shape, data, imag?, isLogical?}`)\n *   - RuntimeChar   (`{mtoc2Tag:"char", value}`)\n *   - struct / class / handle / handle-capture objects — plain JS\n *     objects, optionally with a non-enumerable `mtoc2Class` tag\n *     (class instances) that must survive the clone for method\n *     dispatch\n *\n * Primitives (numbers, booleans, strings, null/undefined) flow\n * through unchanged — they\'re already value-typed in JS.\n */\n\nfunction mtoc2_deep_clone(v) {\n  if (v === null || typeof v !== "object") return v;\n  if (v.mtoc2Tag === "tensor") {\n    const out = {\n      mtoc2Tag: "tensor",\n      shape: v.shape.slice(),\n      data: v.data.slice(),\n    };\n    if (v.imag !== undefined) out.imag = v.imag.slice();\n    if (v.isLogical) out.isLogical = true;\n    return out;\n  }\n  if (v.mtoc2Tag === "char") {\n    return { mtoc2Tag: "char", value: v.value };\n  }\n  if (v.mtoc2Tag === "cell") {\n    return {\n      mtoc2Tag: "cell",\n      shape: v.shape.slice(),\n      data: v.data.map(mtoc2_deep_clone),\n    };\n  }\n  if (v.mtoc2Handle === true) {\n    // Function handle — captures are deep-cloned so a later write\n    // through one alias doesn\'t bleed into another handle\'s frozen\n    // capture snapshot.\n    if (v.kind === "named") {\n      return { mtoc2Handle: true, kind: "named", name: v.name };\n    }\n    const captures = {};\n    for (const k of Object.keys(v.captures)) {\n      captures[k] = mtoc2_deep_clone(v.captures[k]);\n    }\n    return {\n      mtoc2Handle: true,\n      kind: "anon",\n      params: v.params,\n      body: v.body,\n      captures,\n    };\n  }\n  // Plain struct / class instance: walk enumerable fields, then\n  // restore the non-enumerable `mtoc2Class` tag if it was present.\n  const out = {};\n  for (const k of Object.keys(v)) {\n    out[k] = mtoc2_deep_clone(v[k]);\n  }\n  const tag = Object.getOwnPropertyDescriptor(v, "mtoc2Class");\n  if (tag !== undefined) {\n    Object.defineProperty(out, "mtoc2Class", {\n      value: tag.value,\n      enumerable: false,\n      writable: false,\n    });\n  }\n  return out;\n}\n',
  "isequal.js":
    "// JS sibling of `isequal.h`. Same real-numeric equality semantics for\n// the js-aot backend. Tensors carry `shape` / `data` (the JS analogue\n// of the C struct's `dims` / `real`).\n\nfunction mtoc2_isequal_tt(a, b) {\n  if (a.shape.length !== b.shape.length) return 0;\n  let n = 1;\n  for (let i = 0; i < a.shape.length; i++) {\n    if (a.shape[i] !== b.shape[i]) return 0;\n    n *= a.shape[i];\n  }\n  for (let i = 0; i < n; i++) {\n    if (a.data[i] !== b.data[i]) return 0;\n  }\n  return 1;\n}\n\nfunction mtoc2_isequal_st(s, t) {\n  let n = 1;\n  for (let i = 0; i < t.shape.length; i++) n *= t.shape[i];\n  if (n !== 1) return 0;\n  return t.data[0] === s ? 1 : 0;\n}\n",
  "tictoc.js":
    '// JS sibling of `tictoc.h`. Same "one global timer" semantics. Uses\n// `performance.now()` to match numbl\'s wall-clock source; the output\n// format string mirrors numbl\'s `"Elapsed time is %.6f seconds.\\n"`\n// so the cross-runner\'s masking regex still treats both runtimes as\n// equivalent.\n//\n// Module-scope state for the shared `tic` slot. The codegen path\n// inlines this declaration alongside the helpers; the interpreter\n// imports them as ES exports and the same module-scope `let` is the\n// canonical slot.\n\nlet mtoc2_tic_seconds = 0;\n\nfunction mtoc2_now_seconds() {\n  // `performance.now()` returns milliseconds since some monotonic\n  // epoch; divide for seconds. (Node and browsers both supply it on\n  // `globalThis.performance` as of recent versions.)\n  return performance.now() / 1000;\n}\n\nfunction mtoc2_tic() {\n  mtoc2_tic_seconds = mtoc2_now_seconds();\n  return mtoc2_tic_seconds;\n}\n\nfunction mtoc2_toc() {\n  return mtoc2_now_seconds() - mtoc2_tic_seconds;\n}\n\nfunction mtoc2_toc_handle(start_seconds) {\n  return mtoc2_now_seconds() - start_seconds;\n}\n\nfunction mtoc2_toc_print() {\n  const elapsed = mtoc2_toc();\n  $write(`Elapsed time is ${elapsed.toFixed(6)} seconds.\\n`);\n}\n\nfunction mtoc2_toc_handle_print(start_seconds) {\n  const elapsed = mtoc2_now_seconds() - start_seconds;\n  $write(`Elapsed time is ${elapsed.toFixed(6)} seconds.\\n`);\n}\n',
  "tensor.js":
    '// JS sibling of `tensor.h`. The C side defines a struct\n// (`mtoc2_tensor_t`); JS uses a plain object with the same logical\n// fields:\n//\n//   { mtoc2Tag: "tensor", shape: number[], data: Float64Array,\n//     imag?: Float64Array }\n//\n// `mtoc2Tag` lets the interpreter distinguish tensors from plain JS\n// objects (matches the `RuntimeTensor` discriminator in\n// `runtime/value.ts`). `shape` is column-major to match numbl /\n// LAPACK. `imag` is undefined for statically-real tensors.\n//\n// JS has GC, so the C-side ownership protocol\'s `_free` / `_assign`\n// helpers have no JS counterpart — lifetime is automatic. Value\n// semantics aren\'t: an alias-producing assignment (`b = a`, function\n// arg pass) still needs a deep copy or the next member / indexed\n// write through one alias would leak. See `system/deep_clone.js`\n// and the `emitOwnedRhsJs` wrapper in `src/codegen/emitJs.ts`.\n\nfunction mtoc2_tensor_make(shape, data) {\n  let total = 1;\n  for (const s of shape) total *= s;\n  if (data.length !== total) {\n    throw new Error(\n      `mtoc2_tensor_make: shape [${shape.join(",")}] expects ${total} elements, got ${data.length}`\n    );\n  }\n  return { mtoc2Tag: "tensor", shape: shape.slice(), data };\n}\n\nfunction mtoc2_tensor_numel(t) {\n  let n = 1;\n  for (const s of t.shape) n *= s;\n  return n;\n}\n',
  "tensor_alloc.js":
    "// JS sibling of `tensor_alloc.h`. 2-D allocator (R × C). The C side\n// hardcodes a 2-D fast path next to the more general `_alloc_nd`;\n// JS keeps both for structural parity.\n\n\nfunction mtoc2_tensor_alloc(rows, cols) {\n  return mtoc2_tensor_make([rows, cols], new Float64Array(rows * cols));\n}\n",
  "tensor_alloc_complex.js":
    '// JS sibling of `tensor_alloc_complex.h`. Allocate a 2-D complex\n// tensor; both `data` (real) and `imag` lanes are zero-initialised.\n\nfunction mtoc2_tensor_alloc_complex(rows, cols) {\n  if (rows < 0) rows = 0;\n  if (cols < 0) cols = 0;\n  const n = rows * cols;\n  return {\n    mtoc2Tag: "tensor",\n    shape: [rows, cols],\n    data: new Float64Array(n),\n    imag: new Float64Array(n),\n  };\n}\n',
  "tensor_alloc_nd.js":
    "// JS sibling of `tensor_alloc_nd.h`. Allocate a real tensor with\n// the given dimensions (column-major). `data` is initialised to all\n// zeros by Float64Array's default semantics — the same as the C side\n// (which routes through `mtoc2_alloc` + a NULL check; here we trust\n// the engine to keep this allocation tiny enough to not need a\n// guarded path).\n\n\nfunction mtoc2_tensor_alloc_nd(ndim, dims) {\n  const shape = [];\n  for (let i = 0; i < ndim; i++) {\n    // MATLAB / numbl clamp negative dim values to 0 (empty tensor)\n    // rather than aborting. Mirrors the C helper.\n    const d = dims[i] < 0 ? 0 : dims[i];\n    shape.push(d);\n  }\n  let total = 1;\n  for (const s of shape) total *= s;\n  return mtoc2_tensor_make(shape, new Float64Array(total));\n}\n",
  "tensor_alloc_nd_complex.js":
    '// JS sibling of `tensor_alloc_nd_complex.h`. Allocate an N-D complex\n// tensor; both `data` and `imag` lanes are zero-initialised.\n\nfunction mtoc2_tensor_alloc_nd_complex(ndim, dims) {\n  const shape = [];\n  for (let i = 0; i < ndim; i++) {\n    shape.push(dims[i] < 0 ? 0 : dims[i]);\n  }\n  let total = 1;\n  for (const s of shape) total *= s;\n  return {\n    mtoc2Tag: "tensor",\n    shape,\n    data: new Float64Array(total),\n    imag: new Float64Array(total),\n  };\n}\n',
  "tensor_copy_complex.js":
    '// JS sibling of `tensor_copy_complex.h`. Deep-copy a complex tensor\n// (both lanes). The C path needs this because every owned-value\n// produces a freshly-owned result; in JS the GC handles ownership,\n// but we still want a structural deep copy so the source can be\n// mutated independently.\n\nfunction mtoc2_tensor_copy_complex(t) {\n  return {\n    mtoc2Tag: "tensor",\n    shape: t.shape.slice(),\n    data: new Float64Array(t.data),\n    imag:\n      t.imag !== undefined\n        ? new Float64Array(t.imag)\n        : new Float64Array(t.data.length),\n  };\n}\n',
  "tensor_from_matrix.js":
    "// JS sibling of `tensor_from_matrix.h`. Build an R×C tensor from a\n// flat column-major array. The codegen path emits:\n//   mtoc2_tensor_from_matrix([col0_row0, col0_row1, ..., col1_row0,\n//                             col1_row1, ...], rows, cols)\n\n\nfunction mtoc2_tensor_from_matrix(values, rows, cols) {\n  const t = mtoc2_tensor_alloc(rows, cols);\n  for (let i = 0; i < rows * cols; i++) t.data[i] = values[i];\n  return t;\n}\n",
  "tensor_from_matrix_complex.js":
    "// JS sibling of `tensor_from_matrix_complex.h`. Build a rows×cols\n// complex tensor from parallel column-major `re` and `im` arrays.\n\n\nfunction mtoc2_tensor_from_matrix_complex(re, im, rows, cols) {\n  const t = mtoc2_tensor_alloc_complex(rows, cols);\n  const n = rows * cols;\n  for (let i = 0; i < n; i++) {\n    t.data[i] = re[i];\n    t.imag[i] = im[i];\n  }\n  return t;\n}\n",
  "tensor_from_row.js":
    "// JS sibling of `tensor_from_row.h`. Build a 1×N tensor from a JS\n// array (or typed array) of values, copied into a fresh\n// Float64Array. The codegen path passes a plain JS array literal:\n//   mtoc2_tensor_from_row([1, 2, 3], 3)\n//\n// The C side signature is `mtoc2_tensor_from_row(double[], long\n// cols)`, which the C compiler can read inline from a compound\n// literal. The JS analogue is the same shape.\n\n\nfunction mtoc2_tensor_from_row(values, cols) {\n  const t = mtoc2_tensor_alloc(1, cols);\n  for (let i = 0; i < cols; i++) t.data[i] = values[i];\n  return t;\n}\n",
  "tensor_from_row_complex.js":
    "// JS sibling of `tensor_from_row_complex.h`. Build a 1×N complex\n// tensor from parallel `re` and `im` JS arrays (matching the C side's\n// `double[]` sources). The codegen path passes plain JS array\n// literals.\n\n\nfunction mtoc2_tensor_from_row_complex(re, im, n) {\n  const t = mtoc2_tensor_alloc_complex(1, n);\n  for (let i = 0; i < n; i++) {\n    t.data[i] = re[i];\n    t.imag[i] = im[i];\n  }\n  return t;\n}\n",
  "tensor_cat.js":
    '// JS sibling of `tensor_cat.h`. `mtoc2_tensor_cat(dim, nin, xs)`\n// concatenates the inputs along axis `dim` (1-based, like MATLAB /\n// numbl). Inputs may be tensor objects (per the `mtoc2_tensor_make`\n// shape) or plain numbers — scalars are treated as 1×1 tensors.\n//\n// `mtoc2_tensor_cat_complex(dim, nin, xs)` is the complex sibling:\n// inputs may be `{re, im}` scalars, real numbers, real tensors, or\n// complex tensors; result is always a complex tensor.\n//\n// Mirrors numbl\'s `catAlongDim`: empty inputs are dropped (and\n// asymmetrically kept only when their non-cat dims match the reference).\n\n\n\nfunction mtoc2_tensor_cat(dim, nin, xs) {\n  if (dim < 1) {\n    throw new Error(`mtoc2: cat dim ${dim} must be >= 1`);\n  }\n  const dimIdx = dim - 1;\n\n  // Normalize each input: scalar → 1×1; tensor → its shape/data.\n  let maxIn = 2;\n  if (dim > maxIn) maxIn = dim;\n  const parts = [];\n  for (let i = 0; i < nin; i++) {\n    const x = xs[i];\n    if (typeof x === "number") {\n      parts.push({ shape: [1, 1], data: new Float64Array([x]) });\n    } else {\n      const s = x.shape;\n      if (s.length > maxIn) maxIn = s.length;\n      parts.push({ shape: s.slice(), data: x.data });\n    }\n  }\n  const ndim = maxIn;\n  // Pad shapes to ndim with 1s.\n  for (const p of parts) {\n    while (p.shape.length < ndim) p.shape.push(1);\n  }\n\n  // Each input\'s flat size (under padded shape, which equals its raw\n  // numel since trailing 1s preserve column-major layout).\n  for (const p of parts) {\n    let n = 1;\n    for (const d of p.shape) n *= d;\n    p.total = n;\n  }\n\n  // Step 2: find first non-empty input as the reference.\n  let refIdx = -1;\n  for (let i = 0; i < parts.length; i++) {\n    if (parts[i].total > 0) {\n      refIdx = i;\n      break;\n    }\n  }\n  if (refIdx === -1) {\n    // All inputs empty (or none given): canonical [0, 0] empty.\n    return mtoc2_tensor_alloc_nd(2, [0, 0]);\n  }\n\n  const refShape = parts[refIdx].shape;\n\n  // Step 3: keep[] decides which inputs participate.\n  const keep = parts.map((p, i) => {\n    if (p.total > 0) {\n      if (i === refIdx) return true;\n      for (let d = 0; d < ndim; d++) {\n        if (d === dimIdx) continue;\n        if (p.shape[d] !== refShape[d]) {\n          throw new Error(\n            `mtoc2: cat dimension mismatch on dimension ${d + 1}`\n          );\n        }\n      }\n      return true;\n    }\n    // Empty input — keep iff non-cat dims match ref.\n    for (let d = 0; d < ndim; d++) {\n      if (d === dimIdx) continue;\n      if (p.shape[d] !== refShape[d]) return false;\n    }\n    return true;\n  });\n\n  // Step 4: result shape.\n  const resultShape = refShape.slice();\n  let catSum = 0;\n  for (let i = 0; i < parts.length; i++) {\n    if (keep[i]) catSum += parts[i].shape[dimIdx];\n  }\n  resultShape[dimIdx] = catSum;\n\n  const out = mtoc2_tensor_alloc_nd(ndim, resultShape);\n  let resultTotal = 1;\n  for (const d of resultShape) resultTotal *= d;\n  if (resultTotal === 0) return out;\n\n  // Step 5: column-major slab copies.\n  let strideDim = 1;\n  for (let d = 0; d < dimIdx; d++) strideDim *= resultShape[d];\n  let numOuter = 1;\n  for (let d = dimIdx + 1; d < ndim; d++) numOuter *= resultShape[d];\n\n  for (let outer = 0; outer < numOuter; outer++) {\n    let dstOff = outer * strideDim * resultShape[dimIdx];\n    for (let i = 0; i < parts.length; i++) {\n      if (!keep[i]) continue;\n      const srcDimSize = parts[i].shape[dimIdx];\n      const blockSize = strideDim * srcDimSize;\n      const srcOff = outer * blockSize;\n      if (blockSize > 0) {\n        out.data.set(\n          parts[i].data.subarray(srcOff, srcOff + blockSize),\n          dstOff\n        );\n      }\n      dstOff += blockSize;\n    }\n  }\n  return out;\n}\n\nfunction inputLanesComplex(x) {\n  if (typeof x === "number") {\n    return { shape: [1, 1], data: new Float64Array([x]), imag: undefined };\n  }\n  if (typeof x === "object" && x !== null && "re" in x && "im" in x) {\n    return {\n      shape: [1, 1],\n      data: new Float64Array([x.re]),\n      imag: new Float64Array([x.im]),\n    };\n  }\n  return { shape: x.shape.slice(), data: x.data, imag: x.imag };\n}\n\nfunction mtoc2_tensor_cat_complex(dim, nin, xs) {\n  if (dim < 1) {\n    throw new Error(`mtoc2: cat_complex dim ${dim} must be >= 1`);\n  }\n  const dimIdx = dim - 1;\n\n  let maxIn = 2;\n  if (dim > maxIn) maxIn = dim;\n  const parts = [];\n  for (let i = 0; i < nin; i++) {\n    const p = inputLanesComplex(xs[i]);\n    if (p.shape.length > maxIn) maxIn = p.shape.length;\n    parts.push(p);\n  }\n  const ndim = maxIn;\n  for (const p of parts) {\n    while (p.shape.length < ndim) p.shape.push(1);\n  }\n  for (const p of parts) {\n    let n = 1;\n    for (const d of p.shape) n *= d;\n    p.total = n;\n  }\n\n  let refIdx = -1;\n  for (let i = 0; i < parts.length; i++) {\n    if (parts[i].total > 0) {\n      refIdx = i;\n      break;\n    }\n  }\n  if (refIdx === -1) {\n    return mtoc2_tensor_alloc_nd_complex(2, [0, 0]);\n  }\n  const refShape = parts[refIdx].shape;\n\n  const keep = parts.map((p, i) => {\n    if (p.total > 0) {\n      if (i === refIdx) return true;\n      for (let d = 0; d < ndim; d++) {\n        if (d === dimIdx) continue;\n        if (p.shape[d] !== refShape[d]) {\n          throw new Error(\n            `mtoc2: cat_complex dimension mismatch on dimension ${d + 1}`\n          );\n        }\n      }\n      return true;\n    }\n    for (let d = 0; d < ndim; d++) {\n      if (d === dimIdx) continue;\n      if (p.shape[d] !== refShape[d]) return false;\n    }\n    return true;\n  });\n\n  const resultShape = refShape.slice();\n  let catSum = 0;\n  for (let i = 0; i < parts.length; i++) {\n    if (keep[i]) catSum += parts[i].shape[dimIdx];\n  }\n  resultShape[dimIdx] = catSum;\n\n  const out = mtoc2_tensor_alloc_nd_complex(ndim, resultShape);\n  let resultTotal = 1;\n  for (const d of resultShape) resultTotal *= d;\n  if (resultTotal === 0) return out;\n\n  let strideDim = 1;\n  for (let d = 0; d < dimIdx; d++) strideDim *= resultShape[d];\n  let numOuter = 1;\n  for (let d = dimIdx + 1; d < ndim; d++) numOuter *= resultShape[d];\n\n  for (let outer = 0; outer < numOuter; outer++) {\n    let dstOff = outer * strideDim * resultShape[dimIdx];\n    for (let i = 0; i < parts.length; i++) {\n      if (!keep[i]) continue;\n      const srcDimSize = parts[i].shape[dimIdx];\n      const blockSize = strideDim * srcDimSize;\n      const srcOff = outer * blockSize;\n      if (blockSize > 0) {\n        out.data.set(\n          parts[i].data.subarray(srcOff, srcOff + blockSize),\n          dstOff\n        );\n        if (parts[i].imag !== undefined) {\n          out.imag.set(\n            parts[i].imag.subarray(srcOff, srcOff + blockSize),\n            dstOff\n          );\n        }\n      }\n      dstOff += blockSize;\n    }\n  }\n  return out;\n}\n',
  "tensor_cumulative.js":
    "// JS sibling of `tensor_cumulative.h`. Prefix-scan helpers\n// (`cumsum`, `cumprod`) on real and complex tensors. `_dim` returns\n// a freshly-allocated tensor of the SAME shape as the input, scanned\n// along the 1-based `dim` axis. Mirrors numbl's `cumOp`\n// (helpers/reduction/cumulative.ts) with column-major\n// (before × axis × after) fiber traversal.\n//\n// NaN propagates: once acc becomes NaN, every later output along\n// that fiber is NaN. Matches the C side and numbl.\n\n\n\nfunction cumScan(t, dim, init, op) {\n  if (dim < 1) {\n    throw new Error(`cumulative _dim: dim must be >= 1 (got ${dim})`);\n  }\n  const shape = t.shape;\n  const out = mtoc2_tensor_alloc_nd(shape.length, shape.slice());\n  if (dim > shape.length) {\n    out.data.set(t.data);\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < shape.length; i++) after *= shape[i];\n  for (let outer = 0; outer < after; outer++) {\n    const slabBase = outer * before * axis;\n    for (let inner = 0; inner < before; inner++) {\n      let acc = init;\n      for (let k = 0; k < axis; k++) {\n        const idx = slabBase + inner + k * before;\n        acc = op(acc, t.data[idx]);\n        out.data[idx] = acc;\n      }\n    }\n  }\n  return out;\n}\n\nfunction cumScanComplex(t, dim, initRe, initIm, op) {\n  if (dim < 1) {\n    throw new Error(`cumulative_complex _dim: dim must be >= 1 (got ${dim})`);\n  }\n  const shape = t.shape;\n  const out = mtoc2_tensor_alloc_nd_complex(shape.length, shape.slice());\n  const im = t.imag;\n  if (dim > shape.length) {\n    out.data.set(t.data);\n    if (im !== undefined) out.imag.set(im);\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < shape.length; i++) after *= shape[i];\n  for (let outer = 0; outer < after; outer++) {\n    const slabBase = outer * before * axis;\n    for (let inner = 0; inner < before; inner++) {\n      let aRe = initRe;\n      let aIm = initIm;\n      for (let k = 0; k < axis; k++) {\n        const idx = slabBase + inner + k * before;\n        const xRe = t.data[idx];\n        const xIm = im !== undefined ? im[idx] : 0;\n        const next = op(aRe, aIm, xRe, xIm);\n        aRe = next[0];\n        aIm = next[1];\n        out.data[idx] = aRe;\n        out.imag[idx] = aIm;\n      }\n    }\n  }\n  return out;\n}\n\nconst mtoc2_tensor_cumsum_dim = (t, dim) =>\n  cumScan(t, dim, 0, (a, x) => a + x);\nconst mtoc2_tensor_cumprod_dim = (t, dim) =>\n  cumScan(t, dim, 1, (a, x) => a * x);\n\n// cummax / cummin: `Math.max` / `Math.min` propagate NaN, matching\n// numbl's cumOp(Math.max). Seeding with ∓Infinity reproduces the\n// seed-with-first-element behaviour (Math.max(-Infinity, x) === x).\nconst mtoc2_tensor_cummax_dim = (t, dim) =>\n  cumScan(t, dim, -Infinity, Math.max);\nconst mtoc2_tensor_cummin_dim = (t, dim) =>\n  cumScan(t, dim, Infinity, Math.min);\n\nconst mtoc2_tensor_cumsum_complex_dim = (t, dim) =>\n  cumScanComplex(t, dim, 0, 0, (aRe, aIm, xRe, xIm) => [aRe + xRe, aIm + xIm]);\n\nconst mtoc2_tensor_cumprod_complex_dim = (t, dim) =>\n  cumScanComplex(t, dim, 1, 0, (aRe, aIm, xRe, xIm) => [\n    aRe * xRe - aIm * xIm,\n    aRe * xIm + aIm * xRe,\n  ]);\n",
  "tensor_diag.js":
    "// JS sibling of `tensor_diag.h`. Six helpers — `from_scalar`,\n// `construct`, `extract`, and `_complex` siblings of each. Mirrors\n// `diag`'s tensor branch in numbl's `array-manipulation.ts`.\n\n\n\nfunction mtoc2_tensor_diag_from_scalar(v, k) {\n  const absk = Math.abs(k);\n  const m = 1 + absk;\n  const out = mtoc2_tensor_alloc(m, m);\n  const r = k < 0 ? -k : 0;\n  const c = k > 0 ? k : 0;\n  out.data[r + c * m] = v;\n  return out;\n}\n\nfunction mtoc2_tensor_diag_construct(v, k) {\n  const rows = v.shape[0];\n  const cols = v.shape[1];\n  const vecLen = Math.max(rows, cols);\n  const absk = Math.abs(k);\n  const m = vecLen + absk;\n  const out = mtoc2_tensor_alloc(m, m);\n  for (let i = 0; i < vecLen; i++) {\n    const r = k < 0 ? i - k : i;\n    const c = k > 0 ? i + k : i;\n    out.data[r + c * m] = v.data[i];\n  }\n  return out;\n}\n\nfunction mtoc2_tensor_diag_extract(a, k) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const iStart = k < 0 ? -k : 0;\n  const jStart = k > 0 ? k : 0;\n  const diagLen = Math.max(0, Math.min(rows - iStart, cols - jStart));\n  const out = mtoc2_tensor_alloc(diagLen, 1);\n  for (let i = 0; i < diagLen; i++) {\n    const r = iStart + i;\n    const c = jStart + i;\n    out.data[i] = a.data[r + c * rows];\n  }\n  return out;\n}\n\nfunction mtoc2_tensor_diag_from_scalar_complex(re, im, k) {\n  const absk = k < 0 ? -k : k;\n  const m = 1 + absk;\n  const out = mtoc2_tensor_alloc_nd_complex(2, [m, m]);\n  const r = k < 0 ? -k : 0;\n  const c = k > 0 ? k : 0;\n  out.data[r + c * m] = re;\n  out.imag[r + c * m] = im;\n  return out;\n}\n\nfunction mtoc2_tensor_diag_construct_complex(v, k) {\n  const rows = v.shape[0];\n  const cols = v.shape[1];\n  const vecLen = rows > cols ? rows : cols;\n  const absk = k < 0 ? -k : k;\n  const m = vecLen + absk;\n  const out = mtoc2_tensor_alloc_nd_complex(2, [m, m]);\n  const im = v.imag;\n  for (let i = 0; i < vecLen; i++) {\n    const r = k < 0 ? i - k : i;\n    const c = k > 0 ? i + k : i;\n    out.data[r + c * m] = v.data[i];\n    if (im !== undefined) out.imag[r + c * m] = im[i];\n  }\n  return out;\n}\n\nfunction mtoc2_tensor_diag_extract_complex(a, k) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const iStart = k < 0 ? -k : 0;\n  const jStart = k > 0 ? k : 0;\n  const availR = rows - iStart;\n  const availC = cols - jStart;\n  let diagLen = availR < availC ? availR : availC;\n  if (diagLen < 0) diagLen = 0;\n  const out = mtoc2_tensor_alloc_nd_complex(2, [diagLen, 1]);\n  const im = a.imag;\n  for (let i = 0; i < diagLen; i++) {\n    const r = iStart + i;\n    const c = jStart + i;\n    out.data[i] = a.data[r + c * rows];\n    if (im !== undefined) out.imag[i] = im[r + c * rows];\n  }\n  return out;\n}\n",
  "tensor_diff.js":
    "// JS sibling of `tensor_diff.h`. First-order difference along the\n// operating axis, byte-for-byte with numbl's `diffOnce`\n// (helpers/reduction/cumulative.ts). `dim === 0` selects the default\n// axis (row vector → dim 2, else first dim); `dim > 0` is explicit.\n\n\nfunction mtoc2_tensor_diff(a, dim) {\n  const shape = a.shape;\n  let opDim;\n  if (dim > 0) {\n    opDim = dim - 1;\n  } else if (shape.length <= 1 || (shape.length === 2 && shape[0] === 1)) {\n    opDim = shape.length === 2 && shape[0] === 1 ? 1 : 0;\n  } else {\n    opDim = 0;\n  }\n  const dimSize = opDim < shape.length ? shape[opDim] : 1;\n\n  const newDims = shape.slice();\n  if (dimSize <= 1) {\n    if (opDim < newDims.length) newDims[opDim] = 0;\n    while (newDims.length > 2 && newDims[newDims.length - 1] === 1)\n      newDims.pop();\n    return mtoc2_tensor_alloc_nd(newDims.length, newDims);\n  }\n\n  newDims[opDim] = dimSize - 1;\n  while (newDims.length > 2 && newDims[newDims.length - 1] === 1) newDims.pop();\n  const out = mtoc2_tensor_alloc_nd(newDims.length, newDims);\n\n  let innerCount = 1;\n  for (let d = 0; d < opDim; d++) innerCount *= shape[d];\n  let outerCount = 1;\n  for (let d = opDim + 1; d < shape.length; d++) outerCount *= shape[d];\n\n  let outIdx = 0;\n  for (let outer = 0; outer < outerCount; outer++) {\n    for (let k = 0; k < dimSize - 1; k++) {\n      for (let inner = 0; inner < innerCount; inner++) {\n        const base = outer * (dimSize * innerCount) + k * innerCount + inner;\n        out.data[outIdx++] = a.data[base + innerCount] - a.data[base];\n      }\n    }\n  }\n  return out;\n}\n",
  "tensor_dot.js":
    '// JS sibling of tensor_dot.h — dot product on real and complex\n// tensors. Complex variant uses `sum(conj(a) .* b)` matching numbl /\n// MATLAB. A missing `imag` lane is treated as zero.\n\n\n\nfunction mtoc2_dot_real(a, b) {\n  if (a.data.length !== b.data.length) {\n    throw new Error("dot: vectors must be same length");\n  }\n  let acc = 0.0;\n  for (let i = 0; i < a.data.length; i++) acc += a.data[i] * b.data[i];\n  return acc;\n}\n\nfunction mtoc2_dot_real_matrix(a, b) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const r = mtoc2_tensor_alloc_nd(2, [1, cols]);\n  for (let j = 0; j < cols; j++) {\n    let acc = 0.0;\n    for (let i = 0; i < rows; i++) {\n      const off = j * rows + i;\n      acc += a.data[off] * b.data[off];\n    }\n    r.data[j] = acc;\n  }\n  return r;\n}\n\nfunction mtoc2_dot_complex(a, b) {\n  if (a.data.length !== b.data.length) {\n    throw new Error("dot_complex: vectors must be same length");\n  }\n  const aIm = a.imag;\n  const bIm = b.imag;\n  let accRe = 0.0;\n  let accIm = 0.0;\n  for (let i = 0; i < a.data.length; i++) {\n    const aRe = a.data[i];\n    const aXi = aIm !== undefined ? aIm[i] : 0;\n    const bRe = b.data[i];\n    const bXi = bIm !== undefined ? bIm[i] : 0;\n    accRe += aRe * bRe + aXi * bXi;\n    accIm += aRe * bXi - aXi * bRe;\n  }\n  return { re: accRe, im: accIm };\n}\n\nfunction mtoc2_dot_complex_matrix(a, b) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const r = mtoc2_tensor_alloc_nd_complex(2, [1, cols]);\n  const aIm = a.imag;\n  const bIm = b.imag;\n  for (let j = 0; j < cols; j++) {\n    let accRe = 0.0;\n    let accIm = 0.0;\n    for (let i = 0; i < rows; i++) {\n      const off = j * rows + i;\n      const aRe = a.data[off];\n      const aXi = aIm !== undefined ? aIm[off] : 0;\n      const bRe = b.data[off];\n      const bXi = bIm !== undefined ? bIm[off] : 0;\n      accRe += aRe * bRe + aXi * bXi;\n      accIm += aRe * bXi - aXi * bRe;\n    }\n    r.data[j] = accRe;\n    r.imag[j] = accIm;\n  }\n  return r;\n}\n',
  "tensor_elemwise_complex.js":
    "// JS sibling of `tensor_elemwise_complex.h`. Per-op tensor-tensor /\n// tensor-scalar / scalar-tensor / broadcast helpers for complex\n// operands. Mirrors the real elemwise scaffold but each element is a\n// `{re, im}` pair carried by the `data` + `imag` lanes.\n\n\n\n\nfunction cLaneOf(t, idx) {\n  return { re: t.data[idx], im: t.imag !== undefined ? t.imag[idx] : 0 };\n}\n\nfunction cTtKernel(a, b, fn) {\n  const r = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    const v = fn(cLaneOf(a, i), cLaneOf(b, i));\n    r.data[i] = v.re;\n    r.imag[i] = v.im;\n  }\n  return r;\n}\n\nfunction cTsKernel(a, s, fn) {\n  const r = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    const v = fn(cLaneOf(a, i), s);\n    r.data[i] = v.re;\n    r.imag[i] = v.im;\n  }\n  return r;\n}\n\nfunction cStKernel(s, a, fn) {\n  const r = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    const v = fn(s, cLaneOf(a, i));\n    r.data[i] = v.re;\n    r.imag[i] = v.im;\n  }\n  return r;\n}\n\n// Broadcast helper — same shape walk as the real elemwise broadcast\n// path but each cell carries two lanes.\nfunction cBcastKernel(a, b, fn) {\n  const ndim = Math.max(a.shape.length, b.shape.length);\n  const ashape = a.shape.slice();\n  const bshape = b.shape.slice();\n  while (ashape.length < ndim) ashape.push(1);\n  while (bshape.length < ndim) bshape.push(1);\n  const outShape = new Array(ndim);\n  for (let i = 0; i < ndim; i++) {\n    // Singleton axis expands to the other; a 0-size axis vs a 1-size axis\n    // broadcasts to 0, not Math.max's 1 (mirrors the C (adim==1)?bdim:adim).\n    outShape[i] = ashape[i] === 1 ? bshape[i] : ashape[i];\n  }\n  const r = mtoc2_tensor_alloc_nd_complex(ndim, outShape);\n  const aStrides = new Array(ndim);\n  const bStrides = new Array(ndim);\n  let as = 1;\n  let bs = 1;\n  for (let i = 0; i < ndim; i++) {\n    aStrides[i] = ashape[i] === 1 ? 0 : as;\n    bStrides[i] = bshape[i] === 1 ? 0 : bs;\n    as *= ashape[i];\n    bs *= bshape[i];\n  }\n  const idx = new Array(ndim).fill(0);\n  for (let k = 0; k < r.data.length; k++) {\n    let aOff = 0;\n    let bOff = 0;\n    for (let i = 0; i < ndim; i++) {\n      aOff += idx[i] * aStrides[i];\n      bOff += idx[i] * bStrides[i];\n    }\n    const v = fn(cLaneOf(a, aOff), cLaneOf(b, bOff));\n    r.data[k] = v.re;\n    r.imag[k] = v.im;\n    for (let i = 0; i < ndim; i++) {\n      idx[i]++;\n      if (idx[i] < outShape[i]) break;\n      idx[i] = 0;\n    }\n  }\n  return r;\n}\n\n// ── plus ──────────────────────────────────────────────────────────────────\nfunction mtoc2_tensor_plus_complex_tt(a, b) {\n  return cTtKernel(a, b, mtoc2_cadd);\n}\nfunction mtoc2_tensor_plus_complex_ts(a, s) {\n  return cTsKernel(a, s, mtoc2_cadd);\n}\nfunction mtoc2_tensor_plus_complex_bcast_tt(a, b) {\n  return cBcastKernel(a, b, mtoc2_cadd);\n}\n\n// ── minus ─────────────────────────────────────────────────────────────────\nfunction mtoc2_tensor_minus_complex_tt(a, b) {\n  return cTtKernel(a, b, mtoc2_csub);\n}\nfunction mtoc2_tensor_minus_complex_ts(a, s) {\n  return cTsKernel(a, s, mtoc2_csub);\n}\nfunction mtoc2_tensor_minus_complex_st(s, a) {\n  return cStKernel(s, a, mtoc2_csub);\n}\nfunction mtoc2_tensor_minus_complex_bcast_tt(a, b) {\n  return cBcastKernel(a, b, mtoc2_csub);\n}\n\n// ── times ─────────────────────────────────────────────────────────────────\nfunction mtoc2_tensor_times_complex_tt(a, b) {\n  return cTtKernel(a, b, mtoc2_cmul);\n}\nfunction mtoc2_tensor_times_complex_ts(a, s) {\n  return cTsKernel(a, s, mtoc2_cmul);\n}\nfunction mtoc2_tensor_times_complex_bcast_tt(a, b) {\n  return cBcastKernel(a, b, mtoc2_cmul);\n}\n\n// ── rdivide ───────────────────────────────────────────────────────────────\nfunction mtoc2_tensor_rdivide_complex_tt(a, b) {\n  return cTtKernel(a, b, mtoc2_cdiv);\n}\nfunction mtoc2_tensor_rdivide_complex_ts(a, s) {\n  return cTsKernel(a, s, mtoc2_cdiv);\n}\nfunction mtoc2_tensor_rdivide_complex_st(s, a) {\n  return cStKernel(s, a, mtoc2_cdiv);\n}\nfunction mtoc2_tensor_rdivide_complex_bcast_tt(a, b) {\n  return cBcastKernel(a, b, mtoc2_cdiv);\n}\n\n// ── uminus ────────────────────────────────────────────────────────────────\nfunction mtoc2_tensor_uminus_complex(a) {\n  const r = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    const v = mtoc2_cneg(cLaneOf(a, i));\n    r.data[i] = v.re;\n    r.imag[i] = v.im;\n  }\n  return r;\n}\n",
  "tensor_elemwise_real.js":
    "// JS sibling of `tensor_elemwise_real.h`. Per-op tensor-tensor /\n// tensor-scalar / scalar-tensor / broadcast helpers. C macros\n// generate these; JS uses a single generic kernel + thin wrappers\n// to preserve the call-site shape the emitter produces.\n\n\n// uminus on a tensor — separate signature (unary). Kept here because\n// the C side groups it with the elemwise file.\nfunction uminus_kernel(a) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = -a.data[i];\n  return r;\n}\n\nfunction shapes_equal(x, y) {\n  if (x.length !== y.length) return false;\n  for (let i = 0; i < x.length; i++) if (x[i] !== y[i]) return false;\n  return true;\n}\n\nfunction tt_kernel(a, b, fn) {\n  // Same-shape fast path. The emitter picks `_tt` whenever it can't\n  // statically prove broadcasting is needed (unknown dims), so the\n  // shapes must be checked at runtime: equal → elementwise; otherwise\n  // defer to the broadcast kernel, which broadcasts compatible shapes\n  // and errors on incompatible ones. Without this, a runtime mismatch\n  // read past `b` and silently produced NaN.\n  if (!shapes_equal(a.shape, b.shape)) return bcast_kernel(a, b, fn);\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = fn(a.data[i], b.data[i]);\n  return r;\n}\n\nfunction ts_kernel(a, s, fn) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = fn(a.data[i], s);\n  return r;\n}\n\nfunction st_kernel(s, a, fn) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = fn(s, a.data[i]);\n  return r;\n}\n\n// Broadcast helper. Pads the shorter shape with trailing 1s on the\n// right; treats any axis whose size is 1 as a stride-0 axis (so the\n// scalar slot replicates).\nfunction bcast_kernel(a, b, fn) {\n  const ndim = Math.max(a.shape.length, b.shape.length);\n  const ashape = a.shape.slice();\n  const bshape = b.shape.slice();\n  while (ashape.length < ndim) ashape.push(1);\n  while (bshape.length < ndim) bshape.push(1);\n  const outShape = new Array(ndim);\n  for (let i = 0; i < ndim; i++) {\n    // MATLAB broadcast rule: axis sizes must be equal or one must be 1.\n    if (ashape[i] !== bshape[i] && ashape[i] !== 1 && bshape[i] !== 1) {\n      throw new Error(\"Matrix dimensions must agree\");\n    }\n    // A singleton axis expands to the other; otherwise they're equal. Must\n    // NOT use Math.max: a 0-size axis vs a 1-size axis broadcasts to 0, not 1\n    // (mirrors the C twin's (adim==1)?bdim:adim).\n    outShape[i] = ashape[i] === 1 ? bshape[i] : ashape[i];\n  }\n  const r = mtoc2_tensor_alloc_nd(ndim, outShape);\n  // Column-major strides for each operand. A singleton axis gets a\n  // stride of 0 so its index \"doesn't move\".\n  const aStrides = new Array(ndim);\n  const bStrides = new Array(ndim);\n  let as = 1;\n  let bs = 1;\n  for (let i = 0; i < ndim; i++) {\n    aStrides[i] = ashape[i] === 1 ? 0 : as;\n    bStrides[i] = bshape[i] === 1 ? 0 : bs;\n    as *= ashape[i];\n    bs *= bshape[i];\n  }\n  // Walk the output index space column-major. Decompose linear\n  // output index `k` into per-axis subscripts, then accumulate the\n  // operand offsets via the per-axis strides.\n  const idx = new Array(ndim).fill(0);\n  for (let k = 0; k < r.data.length; k++) {\n    let aOff = 0;\n    let bOff = 0;\n    for (let i = 0; i < ndim; i++) {\n      aOff += idx[i] * aStrides[i];\n      bOff += idx[i] * bStrides[i];\n    }\n    r.data[k] = fn(a.data[aOff], b.data[bOff]);\n    // Increment subscripts (column-major: axis 0 varies fastest).\n    for (let i = 0; i < ndim; i++) {\n      idx[i]++;\n      if (idx[i] < outShape[i]) break;\n      idx[i] = 0;\n    }\n  }\n  return r;\n}\n\nfunction mtoc2_tensor_uminus(a) {\n  return uminus_kernel(a);\n}\n\nconst opPlus = (x, y) => x + y;\nconst opMinus = (x, y) => x - y;\nconst opTimes = (x, y) => x * y;\nconst opRdivide = (x, y) => x / y;\n\nfunction mtoc2_tensor_plus_tt(a, b) {\n  return tt_kernel(a, b, opPlus);\n}\nfunction mtoc2_tensor_minus_tt(a, b) {\n  return tt_kernel(a, b, opMinus);\n}\nfunction mtoc2_tensor_times_tt(a, b) {\n  return tt_kernel(a, b, opTimes);\n}\nfunction mtoc2_tensor_rdivide_tt(a, b) {\n  return tt_kernel(a, b, opRdivide);\n}\n\nfunction mtoc2_tensor_plus_ts(a, s) {\n  return ts_kernel(a, s, opPlus);\n}\nfunction mtoc2_tensor_minus_ts(a, s) {\n  return ts_kernel(a, s, opMinus);\n}\nfunction mtoc2_tensor_times_ts(a, s) {\n  return ts_kernel(a, s, opTimes);\n}\nfunction mtoc2_tensor_rdivide_ts(a, s) {\n  return ts_kernel(a, s, opRdivide);\n}\n\n// Non-commutative ops also need scalar-OP-tensor.\nfunction mtoc2_tensor_minus_st(s, a) {\n  return st_kernel(s, a, opMinus);\n}\nfunction mtoc2_tensor_rdivide_st(s, a) {\n  return st_kernel(s, a, opRdivide);\n}\n\nfunction mtoc2_tensor_plus_bcast_tt(a, b) {\n  return bcast_kernel(a, b, opPlus);\n}\nfunction mtoc2_tensor_minus_bcast_tt(a, b) {\n  return bcast_kernel(a, b, opMinus);\n}\nfunction mtoc2_tensor_times_bcast_tt(a, b) {\n  return bcast_kernel(a, b, opTimes);\n}\nfunction mtoc2_tensor_rdivide_bcast_tt(a, b) {\n  return bcast_kernel(a, b, opRdivide);\n}\n",
  "tensor_elemwise_real_fn.js":
    "// JS sibling of `tensor_elemwise_real_fn.h`. Per-op tensor kernels\n// for the function-call binary family (`atan2`, `hypot`, `mod`,\n// `rem`, `power`). Mirrors `tensor_elemwise_real.js`'s structure;\n// only the scalar fn differs.\n\n\nfunction tt(a, b, fn) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = fn(a.data[i], b.data[i]);\n  return r;\n}\nfunction ts(a, s, fn) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = fn(a.data[i], s);\n  return r;\n}\nfunction st(s, a, fn) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = fn(s, a.data[i]);\n  return r;\n}\nfunction bcast(a, b, fn) {\n  const ndim = Math.max(a.shape.length, b.shape.length);\n  const ashape = a.shape.slice();\n  const bshape = b.shape.slice();\n  while (ashape.length < ndim) ashape.push(1);\n  while (bshape.length < ndim) bshape.push(1);\n  const outShape = new Array(ndim);\n  // Singleton axis expands to the other; a 0-size axis vs a 1-size axis\n  // broadcasts to 0, not Math.max's 1 (mirrors the C (adim==1)?bdim:adim).\n  for (let i = 0; i < ndim; i++)\n    outShape[i] = ashape[i] === 1 ? bshape[i] : ashape[i];\n  const r = mtoc2_tensor_alloc_nd(ndim, outShape);\n  const aStrides = new Array(ndim);\n  const bStrides = new Array(ndim);\n  let as = 1;\n  let bs = 1;\n  for (let i = 0; i < ndim; i++) {\n    aStrides[i] = ashape[i] === 1 ? 0 : as;\n    bStrides[i] = bshape[i] === 1 ? 0 : bs;\n    as *= ashape[i];\n    bs *= bshape[i];\n  }\n  const idx = new Array(ndim).fill(0);\n  for (let k = 0; k < r.data.length; k++) {\n    let aOff = 0;\n    let bOff = 0;\n    for (let i = 0; i < ndim; i++) {\n      aOff += idx[i] * aStrides[i];\n      bOff += idx[i] * bStrides[i];\n    }\n    r.data[k] = fn(a.data[aOff], b.data[bOff]);\n    for (let i = 0; i < ndim; i++) {\n      idx[i]++;\n      if (idx[i] < outShape[i]) break;\n      idx[i] = 0;\n    }\n  }\n  return r;\n}\n\n// MATLAB-style mod (sign tracks divisor). Mirrors `mtoc2_mod_real`\n// in the .h. Special-case: `mod(a, 0) = a`.\nfunction mtoc2_mod_real_js(a, b) {\n  if (b === 0) return a;\n  let r = a % b;\n  if (r !== 0 && r < 0 !== b < 0) r += b;\n  return r;\n}\n\n// atan2 / hypot / power\nconst mtoc2_tensor_atan2_tt = (a, b) => tt(a, b, Math.atan2);\nconst mtoc2_tensor_atan2_ts = (a, s) => ts(a, s, Math.atan2);\nconst mtoc2_tensor_atan2_st = (s, a) => st(s, a, Math.atan2);\nconst mtoc2_tensor_atan2_bcast_tt = (a, b) => bcast(a, b, Math.atan2);\n\nconst mtoc2_tensor_hypot_tt = (a, b) => tt(a, b, Math.hypot);\nconst mtoc2_tensor_hypot_ts = (a, s) => ts(a, s, Math.hypot);\n// hypot is commutative — no `_st` in the C side; included for symmetry.\nconst mtoc2_tensor_hypot_bcast_tt = (a, b) => bcast(a, b, Math.hypot);\n\nconst mtoc2_tensor_power_tt = (a, b) => tt(a, b, Math.pow);\nconst mtoc2_tensor_power_ts = (a, s) => ts(a, s, Math.pow);\nconst mtoc2_tensor_power_st = (s, a) => st(s, a, Math.pow);\nconst mtoc2_tensor_power_bcast_tt = (a, b) => bcast(a, b, Math.pow);\n\n// rem: JS `%` matches C `fmod` semantics (sign tracks a).\nconst remFn = (a, b) => a % b;\nconst mtoc2_tensor_rem_tt = (a, b) => tt(a, b, remFn);\nconst mtoc2_tensor_rem_ts = (a, s) => ts(a, s, remFn);\nconst mtoc2_tensor_rem_st = (s, a) => st(s, a, remFn);\nconst mtoc2_tensor_rem_bcast_tt = (a, b) => bcast(a, b, remFn);\n\n// mod: MATLAB convention (sign tracks b).\nconst mtoc2_tensor_mod_tt = (a, b) => tt(a, b, mtoc2_mod_real_js);\nconst mtoc2_tensor_mod_ts = (a, s) => ts(a, s, mtoc2_mod_real_js);\nconst mtoc2_tensor_mod_st = (s, a) => st(s, a, mtoc2_mod_real_js);\nconst mtoc2_tensor_mod_bcast_tt = (a, b) =>\n  bcast(a, b, mtoc2_mod_real_js);\n",
  "tensor_eye.js":
    "// JS sibling of `tensor_eye.h`. Build an m×n identity matrix.\n\n\nfunction mtoc2_eye_rect(rows, cols) {\n  const out = mtoc2_tensor_alloc(rows, cols);\n  const m = Math.min(rows, cols);\n  for (let i = 0; i < m; i++) out.data[i + i * rows] = 1;\n  return out;\n}\n\nfunction mtoc2_eye_square(n) {\n  return mtoc2_eye_rect(n, n);\n}\n",
  "tensor_fill_nd.js":
    "// JS sibling of `tensor_fill_nd.h`. Like zeros/ones but takes the\n// fill value as a leading argument — used by the `nan` / `Inf` shape-\n// constructor branches and by `repmat(scalar, ...)`. Complex variant\n// takes `(re, im)` and fills both lanes.\n\n\n\nfunction mtoc2_tensor_fill_nd(value, ndim, dims) {\n  const t = mtoc2_tensor_alloc_nd(ndim, dims);\n  t.data.fill(value);\n  return t;\n}\n\nfunction mtoc2_tensor_fill_nd_complex(re, im, ndim, dims) {\n  const t = mtoc2_tensor_alloc_nd_complex(ndim, dims);\n  t.data.fill(re);\n  t.imag.fill(im);\n  return t;\n}\n",
  "tensor_fill_square.js":
    "// JS sibling of `tensor_fill_square.h`. Single-eval helper for\n// `nan(n)` / `Inf(n)` style square-fill constructors.\n\n\nfunction mtoc2_tensor_fill_square(value, n) {\n  return mtoc2_tensor_fill_nd(value, 2, [n, n]);\n}\n",
  "tensor_flip.js":
    "// JS sibling of `tensor_flip.h`. Two helpers:\n//   - `mtoc2_tensor_flip(t, dimIdx)` — real-input variant.\n//   - `mtoc2_tensor_flip_complex(t, dimIdx)` — walks both lanes;\n//     if `imag` is undefined the output imag stays zero.\n// Out-of-range axis acts as a deep-copy no-op in both.\n\n\n\nfunction mtoc2_tensor_flip(a, dimIdx) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  const axisSize = dimIdx >= 0 && dimIdx < a.shape.length ? a.shape[dimIdx] : 1;\n  if (axisSize <= 1) {\n    r.data.set(a.data);\n    return r;\n  }\n  let strideDim = 1;\n  for (let d = 0; d < dimIdx; d++) strideDim *= a.shape[d];\n  const slabSize = strideDim * axisSize;\n  const total = r.data.length;\n  const numOuter = total / slabSize;\n  for (let outer = 0; outer < numOuter; outer++) {\n    const base = outer * slabSize;\n    for (let k = 0; k < axisSize; k++) {\n      const srcOff = base + k * strideDim;\n      const dstOff = base + (axisSize - 1 - k) * strideDim;\n      for (let i = 0; i < strideDim; i++) {\n        r.data[dstOff + i] = a.data[srcOff + i];\n      }\n    }\n  }\n  return r;\n}\n\nfunction mtoc2_tensor_flip_complex(a, dimIdx) {\n  const r = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  const im = a.imag;\n  const axisSize = dimIdx >= 0 && dimIdx < a.shape.length ? a.shape[dimIdx] : 1;\n  if (axisSize <= 1) {\n    r.data.set(a.data);\n    if (im !== undefined) r.imag.set(im);\n    return r;\n  }\n  let strideDim = 1;\n  for (let d = 0; d < dimIdx; d++) strideDim *= a.shape[d];\n  const slabSize = strideDim * axisSize;\n  const total = r.data.length;\n  const numOuter = total / slabSize;\n  for (let outer = 0; outer < numOuter; outer++) {\n    const base = outer * slabSize;\n    for (let k = 0; k < axisSize; k++) {\n      const srcOff = base + k * strideDim;\n      const dstOff = base + (axisSize - 1 - k) * strideDim;\n      for (let i = 0; i < strideDim; i++) {\n        r.data[dstOff + i] = a.data[srcOff + i];\n        if (im !== undefined) r.imag[dstOff + i] = im[srcOff + i];\n      }\n    }\n  }\n  return r;\n}\n",
  "tensor_imag_all_zero.js":
    "// JS sibling of `tensor_imag_all_zero.h`. True when a tensor carries no\n// imaginary content (no imag lane, or every imag element exactly zero).\n// `isreal` uses this for complex-typed tensors the JIT could not prove\n// real at compile time, so it reports realness by value — matching the\n// interpreter and the complex-scalar `v.im === 0` rule.\nfunction mtoc2_tensor_imag_all_zero(a) {\n  if (a.imag === undefined) return true;\n  for (let i = 0; i < a.imag.length; i++) {\n    if (a.imag[i] !== 0) return false;\n  }\n  return true;\n}\n",
  "tensor_linspace.js":
    "// JS sibling of `tensor_linspace.h`. Build a 1×n row tensor of n\n// linearly-spaced values from `a` to `b`.\n\n\nfunction mtoc2_tensor_linspace(a, b, n) {\n  if (n < 0) n = 0;\n  const out = mtoc2_tensor_alloc(1, n);\n  if (n === 0) return out;\n  if (n === 1) {\n    out.data[0] = b;\n    return out;\n  }\n  out.data[0] = a;\n  out.data[n - 1] = b;\n  for (let i = 1; i < n - 1; i++) {\n    out.data[i] = a + ((b - a) * i) / (n - 1);\n  }\n  if ((n & 1) === 1 && !Number.isFinite(a) && !Number.isFinite(b)) {\n    const sa = Math.sign(a);\n    const sb = Math.sign(b);\n    if (sa !== sb) out.data[(n - 1) / 2] = 0;\n  }\n  return out;\n}\n",
  "tensor_logical_real.js":
    '// JS sibling of `tensor_logical_real.h`. Elementwise logical NOT on\n// real and complex tensors. Real input: `out[i] = (in[i] == 0) ? 1 : 0`.\n// Complex input: fires "true" iff both lanes are exactly zero.\n// Result is logical-tagged in both cases.\n\n\nfunction mtoc2_tensor_not(a) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    r.data[i] = a.data[i] === 0 ? 1 : 0;\n  }\n  // Tag as logical so `a(mask)` / `M(:, mask)` etc. take the mask\n  // path in the interpreter (and js-aot, when wired). The tensor\n  // alloc helpers return plain numeric tensors; we mutate the field\n  // here rather than threading a parameter through every allocator.\n  r.isLogical = true;\n  return r;\n}\n\nfunction mtoc2_tensor_not_complex(a) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  const im = a.imag;\n  for (let i = 0; i < r.data.length; i++) {\n    const re = a.data[i];\n    const v = im !== undefined ? im[i] : 0;\n    r.data[i] = re === 0 && v === 0 ? 1 : 0;\n  }\n  r.isLogical = true;\n  return r;\n}\n',
  "tensor_logspace.js":
    "// JS sibling of `tensor_logspace.h`. Build a 1×n row tensor of n\n// logarithmically-spaced values from 10^a to 10^b. Byte-for-byte with\n// numbl's interpreter `logspace`, including the MATLAB special case\n// where an upper limit of exactly `pi` makes the last point `pi`.\n\n\nfunction mtoc2_tensor_logspace(a, b, n) {\n  if (n <= 0) return mtoc2_tensor_alloc(1, 0);\n  const isPi = b === Math.PI;\n  const endVal = isPi ? Math.PI : Math.pow(10, b);\n  const out = mtoc2_tensor_alloc(1, n);\n  if (n === 1) {\n    out.data[0] = endVal;\n    return out;\n  }\n  if (isPi) {\n    const logStart = Math.log10(Math.pow(10, a));\n    const logEnd = Math.log10(Math.PI);\n    for (let i = 0; i < n; i++) {\n      const t = logStart + ((logEnd - logStart) * i) / (n - 1);\n      out.data[i] = Math.pow(10, t);\n    }\n  } else {\n    for (let i = 0; i < n; i++) {\n      const t = a + ((b - a) * i) / (n - 1);\n      out.data[i] = Math.pow(10, t);\n    }\n  }\n  return out;\n}\n",
  "tensor_meshgrid.js":
    "// JS sibling of `tensor_meshgrid.h`. MATLAB-style coordinate grid.\n\n\nfunction numel(t) {\n  let n = 1;\n  for (const s of t.shape) n *= s;\n  return n;\n}\n\nfunction mtoc2_meshgrid_x(x, y) {\n  const M = numel(x);\n  const N = numel(y);\n  const X = mtoc2_tensor_alloc(N, M);\n  for (let j = 0; j < M; j++) {\n    const xj = x.data[j];\n    for (let i = 0; i < N; i++) X.data[i + j * N] = xj;\n  }\n  return X;\n}\n\nfunction mtoc2_meshgrid(x, y) {\n  const M = numel(x);\n  const N = numel(y);\n  const X = mtoc2_tensor_alloc(N, M);\n  const Y = mtoc2_tensor_alloc(N, M);\n  for (let j = 0; j < M; j++) {\n    const xj = x.data[j];\n    for (let i = 0; i < N; i++) {\n      X.data[i + j * N] = xj;\n      Y.data[i + j * N] = y.data[i];\n    }\n  }\n  return { X, Y };\n}\n\nfunction mtoc2_meshgrid_1arg(x) {\n  return mtoc2_meshgrid(x, x);\n}\n",
  "tensor_mtimes_complex.js":
    '// JS sibling of `tensor_mtimes_complex.h`. 2-D matrix multiplication\n// producing a complex result. Handles all three mixed cases\n// (complex × complex, complex × real, real × complex) by treating an\n// undefined imag lane as implicit zeros.\n\n\nfunction mtoc2_tensor_mtimes_complex_scalar(a, b) {\n  if (a.shape.length !== 2 || b.shape.length !== 2) {\n    throw new Error("mtoc2_tensor_mtimes_complex_scalar: inputs must be 2-D");\n  }\n  if (a.shape[0] !== 1 || b.shape[1] !== 1) {\n    throw new Error("mtoc2_tensor_mtimes_complex_scalar: requires 1×k * k×1");\n  }\n  if (a.shape[1] !== b.shape[0]) {\n    throw new Error("mtoc2_tensor_mtimes_complex_scalar: inner-dim mismatch");\n  }\n  const k = a.shape[1];\n  const aim = a.imag;\n  const bim = b.imag;\n  let accR = 0;\n  let accI = 0;\n  for (let p = 0; p < k; p++) {\n    const ar = a.data[p];\n    const ai = aim !== undefined ? aim[p] : 0;\n    const br = b.data[p];\n    const bi = bim !== undefined ? bim[p] : 0;\n    accR += ar * br - ai * bi;\n    accI += ar * bi + ai * br;\n  }\n  return { re: accR, im: accI };\n}\n\nfunction mtoc2_tensor_mtimes_complex(a, b) {\n  if (a.shape.length !== 2 || b.shape.length !== 2) {\n    throw new Error("mtoc2_tensor_mtimes_complex: inputs must be 2-D");\n  }\n  const m = a.shape[0];\n  const k = a.shape[1];\n  const k2 = b.shape[0];\n  const n = b.shape[1];\n  if (k !== k2) {\n    throw new Error(\n      `mtoc2_tensor_mtimes_complex: inner-dim mismatch (${m}×${k} * ${k2}×${n})`\n    );\n  }\n  const out = mtoc2_tensor_alloc_complex(m, n);\n  const aim = a.imag;\n  const bim = b.imag;\n  for (let j = 0; j < n; j++) {\n    for (let i = 0; i < m; i++) {\n      let accR = 0;\n      let accI = 0;\n      for (let p = 0; p < k; p++) {\n        const ar = a.data[i + p * m];\n        const ai = aim !== undefined ? aim[i + p * m] : 0;\n        const br = b.data[p + j * k];\n        const bi = bim !== undefined ? bim[p + j * k] : 0;\n        accR += ar * br - ai * bi;\n        accI += ar * bi + ai * br;\n      }\n      out.data[i + j * m] = accR;\n      out.imag[i + j * m] = accI;\n    }\n  }\n  return out;\n}\n',
  "tensor_mtimes_real.js":
    '// JS sibling of `tensor_mtimes_real.h`. Real 2-D matrix\n// multiplication A * B. Column-major in, column-major out.\n// `mtoc2_tensor_mtimes_real_scalar` mirrors the C 1×k * k×1\n// inner-product path that returns a bare number instead of a\n// freshly-allocated 1×1 tensor.\n\n\nfunction mtoc2_tensor_mtimes_real(a, b) {\n  if (a.shape.length !== 2 || b.shape.length !== 2) {\n    throw new Error("mtoc2_tensor_mtimes_real: inputs must be 2-D");\n  }\n  const m = a.shape[0];\n  const k = a.shape[1];\n  const k2 = b.shape[0];\n  const n = b.shape[1];\n  if (k !== k2) {\n    throw new Error(\n      `mtoc2_tensor_mtimes_real: inner dimensions disagree (${k} vs ${k2})`\n    );\n  }\n  // Optional WASM LAPACK accelerator. When a host installs `$matmulAccel`\n  // (browser worker, after loading a bridge), delegate the multiply; a\n  // falsy return means "declined" (too small / error) and we fall through\n  // to the JS loop below. `typeof` guard is safe even when unset.\n  if (typeof $matmulAccel === "function") {\n    const accel = $matmulAccel(a.data, m, k, b.data, n);\n    if (accel) {\n      const acc = mtoc2_tensor_alloc(m, n);\n      acc.data.set(accel);\n      return acc;\n    }\n  }\n  const r = mtoc2_tensor_alloc(m, n);\n  for (let j = 0; j < n; j++) {\n    for (let i = 0; i < m; i++) {\n      let s = 0;\n      for (let p = 0; p < k; p++) {\n        s += a.data[i + p * m] * b.data[p + j * k];\n      }\n      r.data[i + j * m] = s;\n    }\n  }\n  return r;\n}\n\nfunction mtoc2_tensor_mtimes_real_scalar(a, b) {\n  if (a.shape.length !== 2 || b.shape.length !== 2) {\n    throw new Error("mtoc2_tensor_mtimes_real_scalar: inputs must be 2-D");\n  }\n  // The translate-time type system has already proven a is 1×k and b\n  // is k×1; this helper trusts that and just runs the inner product.\n  const k = a.shape[1];\n  let s = 0;\n  for (let p = 0; p < k; p++) {\n    s += a.data[p * a.shape[0]] * b.data[p];\n  }\n  return s;\n}\n',
  "tensor_norm.js":
    "// JS sibling of `tensor_norm.h`. Vector norms for real and complex\n// tensors. The complex variants tolerate a missing imag lane (real\n// tensor flowing through a complex-typed norm route) by treating it\n// as zero.\n\nfunction mtoc2_norm2_real(a) {\n  let acc = 0;\n  for (let i = 0; i < a.data.length; i++) {\n    const x = a.data[i];\n    acc += x * x;\n  }\n  return Math.sqrt(acc);\n}\n\nfunction mtoc2_norm2_complex(a) {\n  let acc = 0;\n  const im = a.imag;\n  for (let i = 0; i < a.data.length; i++) {\n    const re = a.data[i];\n    const imv = im !== undefined ? im[i] : 0;\n    acc += re * re + imv * imv;\n  }\n  return Math.sqrt(acc);\n}\n\nfunction mtoc2_norm_p_real(a, p) {\n  const n = a.data.length;\n  if (n === 0) return 0;\n  if (p === Infinity) {\n    let m = 0;\n    for (let i = 0; i < n; i++) {\n      const x = Math.abs(a.data[i]);\n      if (x > m) m = x;\n    }\n    return m;\n  }\n  if (p === -Infinity) {\n    let m = Math.abs(a.data[0]);\n    for (let i = 1; i < n; i++) {\n      const x = Math.abs(a.data[i]);\n      if (x < m) m = x;\n    }\n    return m;\n  }\n  if (p === 1) {\n    let acc = 0;\n    for (let i = 0; i < n; i++) acc += Math.abs(a.data[i]);\n    return acc;\n  }\n  if (p === 2) return mtoc2_norm2_real(a);\n  let acc = 0;\n  for (let i = 0; i < n; i++) acc += Math.pow(Math.abs(a.data[i]), p);\n  return Math.pow(acc, 1 / p);\n}\n\nfunction mtoc2_norm_p_complex(a, p) {\n  const n = a.data.length;\n  if (n === 0) return 0;\n  const im = a.imag;\n  const abs = i => {\n    const re = a.data[i];\n    const imv = im !== undefined ? im[i] : 0;\n    return Math.hypot(re, imv);\n  };\n  if (p === Infinity) {\n    let m = 0;\n    for (let i = 0; i < n; i++) {\n      const x = abs(i);\n      if (x > m) m = x;\n    }\n    return m;\n  }\n  if (p === -Infinity) {\n    let m = abs(0);\n    for (let i = 1; i < n; i++) {\n      const x = abs(i);\n      if (x < m) m = x;\n    }\n    return m;\n  }\n  if (p === 2) return mtoc2_norm2_complex(a);\n  if (p === 1) {\n    let acc = 0;\n    for (let i = 0; i < n; i++) acc += abs(i);\n    return acc;\n  }\n  let acc = 0;\n  for (let i = 0; i < n; i++) acc += Math.pow(abs(i), p);\n  return Math.pow(acc, 1 / p);\n}\n",
  "tensor_ones_nd.js":
    "// JS sibling of `tensor_ones_nd.h`. Fill the freshly-allocated\n// tensor with `1.0`.\n\n\nfunction mtoc2_tensor_ones_nd(ndim, dims) {\n  const t = mtoc2_tensor_alloc_nd(ndim, dims);\n  t.data.fill(1);\n  return t;\n}\n",
  "tensor_ones_square.js":
    "// JS sibling of `tensor_ones_square.h`. See `tensor_zeros_square.js`\n// for the rationale.\n\n\nfunction mtoc2_tensor_ones_square(n) {\n  return mtoc2_tensor_ones_nd(2, [n, n]);\n}\n",
  "tensor_predicate.js":
    '// JS sibling of `tensor_predicate.h`. Real-tensor → logical-tensor\n// predicate kernels for the js-aot backend, plus their `_complex`\n// siblings (each reads `imag[i]` when defined, treats it as 0\n// otherwise). Result carries `isLogical: true` so downstream index-\n// slot resolution treats it as a mask.\n\nfunction pred_kernel(a, fn) {\n  const out = new Float64Array(a.data.length);\n  for (let i = 0; i < a.data.length; i++) out[i] = fn(a.data[i]) ? 1 : 0;\n  return {\n    mtoc2Tag: "tensor",\n    shape: a.shape.slice(),\n    data: out,\n    isLogical: true,\n  };\n}\n\nfunction pred_kernel_complex(a, fn) {\n  const out = new Float64Array(a.data.length);\n  const im = a.imag;\n  for (let i = 0; i < a.data.length; i++) {\n    out[i] = fn(a.data[i], im !== undefined ? im[i] : 0) ? 1 : 0;\n  }\n  return {\n    mtoc2Tag: "tensor",\n    shape: a.shape.slice(),\n    data: out,\n    isLogical: true,\n  };\n}\n\nfunction mtoc2_tensor_isnan(a) {\n  return pred_kernel(a, Number.isNaN);\n}\n\nfunction mtoc2_tensor_logical(a) {\n  return pred_kernel(a, x => x !== 0);\n}\n\nfunction mtoc2_tensor_isinf(a) {\n  return pred_kernel(a, x => x === Infinity || x === -Infinity);\n}\n\nfunction mtoc2_tensor_isfinite(a) {\n  return pred_kernel(a, Number.isFinite);\n}\n\nfunction mtoc2_tensor_isnan_complex(a) {\n  return pred_kernel_complex(\n    a,\n    (re, im) => Number.isNaN(re) || Number.isNaN(im)\n  );\n}\n\nfunction mtoc2_tensor_isinf_complex(a) {\n  const isInf = x => x === Infinity || x === -Infinity;\n  return pred_kernel_complex(a, (re, im) => isInf(re) || isInf(im));\n}\n\nfunction mtoc2_tensor_isfinite_complex(a) {\n  return pred_kernel_complex(\n    a,\n    (re, im) => Number.isFinite(re) && Number.isFinite(im)\n  );\n}\n',
  "tensor_reduce_complex.js":
    '// JS sibling of `tensor_reduce_complex.h`. Complex-tensor reductions.\n// Mirrors the real reducer shape (sum/prod/mean → complex; min/max\n// → complex via magnitude+atan2 tiebreak; any/all → real).\n\n\n\nfunction cSqueezeTrailing(dims) {\n  while (dims.length > 2 && dims[dims.length - 1] === 1) dims.pop();\n  return dims;\n}\n\nfunction cReduceLaneIm(t, i) {\n  return t.imag !== undefined ? t.imag[i] : 0;\n}\n\n// True when the tensor carries no imaginary content (no lane, or every\n// element zero). Such a tensor is real in value — min/max must order by\n// value, not magnitude, to match the interpreter and MATLAB on real data.\nfunction cReduceAllImagZero(t) {\n  if (t.imag === undefined) return true;\n  for (let i = 0; i < t.imag.length; i++) {\n    if (t.imag[i] !== 0) return false;\n  }\n  return true;\n}\n\n// Numeric (sum/prod/mean) — complex accumulator { re, im }.\nfunction complexAccumAll(t, init, accum, finalize) {\n  let acc = { ...init };\n  for (let i = 0; i < t.data.length; i++) {\n    acc = accum(acc, { re: t.data[i], im: cReduceLaneIm(t, i) });\n  }\n  return finalize(acc, t.data.length);\n}\n\nfunction complexAccumDim(t, dim, init, accum, finalize) {\n  if (dim < 1) throw new Error(`reducer _dim: dim must be >= 1 (got ${dim})`);\n  if (dim > t.shape.length) {\n    // No-op axis: return a fresh complex copy.\n    const out = mtoc2_tensor_alloc_nd_complex(t.shape.length, t.shape.slice());\n    out.data.set(t.data);\n    if (t.imag !== undefined) out.imag.set(t.imag);\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = t.shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= t.shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < t.shape.length; i++) after *= t.shape[i];\n  const outDims = t.shape.slice();\n  outDims[dimIdx] = 1;\n  cSqueezeTrailing(outDims);\n  const out = mtoc2_tensor_alloc_nd_complex(outDims.length, outDims);\n  for (let aft = 0; aft < after; aft++) {\n    for (let bef = 0; bef < before; bef++) {\n      const base = aft * before * axis + bef;\n      let acc = { ...init };\n      for (let k = 0; k < axis; k++) {\n        const off = base + k * before;\n        acc = accum(acc, { re: t.data[off], im: cReduceLaneIm(t, off) });\n      }\n      const fin = finalize(acc, axis);\n      const dst = aft * before + bef;\n      out.data[dst] = fin.re;\n      out.imag[dst] = fin.im;\n    }\n  }\n  return out;\n}\n\nconst cSumInit = { re: 0, im: 0 };\nconst cProdInit = { re: 1, im: 0 };\nconst cSumAccum = (a, x) => ({ re: a.re + x.re, im: a.im + x.im });\nconst cProdAccum = (a, x) => ({\n  re: a.re * x.re - a.im * x.im,\n  im: a.re * x.im + a.im * x.re,\n});\nconst cIdFinalize = a => a;\nconst cMeanFinalize = (a, n) =>\n  n === 0 ? { re: NaN, im: NaN } : { re: a.re / n, im: a.im / n };\n\nconst mtoc2_sum_complex_all = t =>\n  complexAccumAll(t, cSumInit, cSumAccum, cIdFinalize);\nconst mtoc2_sum_complex_dim = (t, d) =>\n  complexAccumDim(t, d, cSumInit, cSumAccum, cIdFinalize);\nconst mtoc2_prod_complex_all = t =>\n  complexAccumAll(t, cProdInit, cProdAccum, cIdFinalize);\nconst mtoc2_prod_complex_dim = (t, d) =>\n  complexAccumDim(t, d, cProdInit, cProdAccum, cIdFinalize);\nconst mtoc2_mean_complex_all = t =>\n  complexAccumAll(t, cSumInit, cSumAccum, cMeanFinalize);\nconst mtoc2_mean_complex_dim = (t, d) =>\n  complexAccumDim(t, d, cSumInit, cSumAccum, cMeanFinalize);\n\n// Min / max — magnitude compare with atan2 tiebreak (numbl\'s\n// complexIsBetter). Skip NaN-lane elements; result is complex.\nfunction complexMinmaxAll(t, cmp) {\n  const realMode = cReduceAllImagZero(t);\n  let found = false;\n  let mRe = NaN;\n  let mIm = 0;\n  for (let i = 0; i < t.data.length; i++) {\n    const xr = t.data[i];\n    const xi = cReduceLaneIm(t, i);\n    if (xr !== xr || xi !== xi) continue;\n    const better = realMode\n      ? cmp === "<"\n        ? xr < mRe\n        : xr > mRe\n      : complexBetter(xr, xi, mRe, mIm, cmp);\n    if (!found || better) {\n      mRe = xr;\n      mIm = xi;\n      found = true;\n    }\n  }\n  return { re: mRe, im: mIm };\n}\n\nfunction complexBetter(aRe, aIm, bRe, bIm, cmp) {\n  const absA = Math.hypot(aRe, aIm);\n  const absB = Math.hypot(bRe, bIm);\n  if (absA !== absB) return cmp === "<" ? absA < absB : absA > absB;\n  return cmp === "<"\n    ? Math.atan2(aIm, aRe) < Math.atan2(bIm, bRe)\n    : Math.atan2(aIm, aRe) > Math.atan2(bIm, bRe);\n}\n\nfunction complexMinmaxDim(t, dim, cmp) {\n  if (dim < 1) throw new Error(`reducer _dim: dim must be >= 1 (got ${dim})`);\n  if (dim > t.shape.length) {\n    const out = mtoc2_tensor_alloc_nd_complex(t.shape.length, t.shape.slice());\n    out.data.set(t.data);\n    if (t.imag !== undefined) out.imag.set(t.imag);\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const realMode = cReduceAllImagZero(t);\n  const axis = t.shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= t.shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < t.shape.length; i++) after *= t.shape[i];\n  const outDims = t.shape.slice();\n  outDims[dimIdx] = 1;\n  cSqueezeTrailing(outDims);\n  const out = mtoc2_tensor_alloc_nd_complex(outDims.length, outDims);\n  for (let aft = 0; aft < after; aft++) {\n    for (let bef = 0; bef < before; bef++) {\n      const base = aft * before * axis + bef;\n      let found = false;\n      let mRe = NaN;\n      let mIm = 0;\n      for (let k = 0; k < axis; k++) {\n        const off = base + k * before;\n        const xr = t.data[off];\n        const xi = cReduceLaneIm(t, off);\n        if (xr !== xr || xi !== xi) continue;\n        const better = realMode\n          ? cmp === "<"\n            ? xr < mRe\n            : xr > mRe\n          : complexBetter(xr, xi, mRe, mIm, cmp);\n        if (!found || better) {\n          mRe = xr;\n          mIm = xi;\n          found = true;\n        }\n      }\n      const dst = aft * before + bef;\n      out.data[dst] = mRe;\n      out.imag[dst] = mIm;\n    }\n  }\n  return out;\n}\n\nconst mtoc2_min_complex_all = t => complexMinmaxAll(t, "<");\nconst mtoc2_min_complex_dim = (t, d) => complexMinmaxDim(t, d, "<");\nconst mtoc2_max_complex_all = t => complexMinmaxAll(t, ">");\nconst mtoc2_max_complex_dim = (t, d) => complexMinmaxDim(t, d, ">");\n\n// any / all — real result; toBool per element (either lane nonzero).\nfunction complexLogicalAll(t, emptyResult, shortPredicate) {\n  if (t.data.length === 0) return emptyResult;\n  for (let i = 0; i < t.data.length; i++) {\n    const xr = t.data[i];\n    const xi = cReduceLaneIm(t, i);\n    const x = xr !== 0 || xi !== 0;\n    if (shortPredicate(x)) return emptyResult === 1 ? 0 : 1;\n  }\n  return emptyResult;\n}\n\nfunction complexLogicalDim(t, dim, emptyResult, shortPredicate) {\n  if (dim < 1) throw new Error(`reducer _dim: dim must be >= 1 (got ${dim})`);\n  if (dim > t.shape.length) {\n    const out = mtoc2_tensor_alloc_nd(t.shape.length, t.shape.slice());\n    for (let i = 0; i < t.data.length; i++) {\n      const xr = t.data[i];\n      const xi = cReduceLaneIm(t, i);\n      out.data[i] = xr !== 0 || xi !== 0 ? 1 : 0;\n    }\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = t.shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= t.shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < t.shape.length; i++) after *= t.shape[i];\n  const outDims = t.shape.slice();\n  outDims[dimIdx] = 1;\n  cSqueezeTrailing(outDims);\n  const out = mtoc2_tensor_alloc_nd(outDims.length, outDims);\n  for (let aft = 0; aft < after; aft++) {\n    for (let bef = 0; bef < before; bef++) {\n      const base = aft * before * axis + bef;\n      let res = emptyResult;\n      for (let k = 0; k < axis; k++) {\n        const off = base + k * before;\n        const x = t.data[off] !== 0 || cReduceLaneIm(t, off) !== 0;\n        if (shortPredicate(x)) {\n          res = emptyResult === 1 ? 0 : 1;\n          break;\n        }\n      }\n      out.data[aft * before + bef] = res;\n    }\n  }\n  return out;\n}\n\nconst cAnyShort = x => x;\nconst cAllShort = x => !x;\nconst mtoc2_any_complex_all = t => complexLogicalAll(t, 0, cAnyShort);\nconst mtoc2_any_complex_dim = (t, d) =>\n  complexLogicalDim(t, d, 0, cAnyShort);\nconst mtoc2_all_complex_all = t => complexLogicalAll(t, 1, cAllShort);\nconst mtoc2_all_complex_dim = (t, d) =>\n  complexLogicalDim(t, d, 1, cAllShort);\n',
  "tensor_reduce_real.js":
    '// JS sibling of `tensor_reduce_real.h`. Real-tensor reductions —\n// `_all` returns a scalar; `_dim` returns a freshly-allocated tensor\n// reduced along the 1-based `dim` axis. Mirrors numbl\'s\n// `forEachSlice` semantics with column-major (before × axis × after)\n// traversal.\n//\n// Output shape rule for `_dim`: input dims with `dims[dim-1] = 1`,\n// then trailing singletons stripped subject to a 2-axis floor.\n\n\nfunction squeeze_trailing(dims) {\n  while (dims.length > 2 && dims[dims.length - 1] === 1) dims.pop();\n  return dims;\n}\n\n// Accumulator-based reducer (`sum`, `prod`, `mean`). `init` seeds\n// the running value; `accum(a, x)` is the per-element step;\n// `finalize(a, n)` is the post-loop transform.\nfunction accum_all(t, init, accum, finalize) {\n  let acc = init;\n  for (let i = 0; i < t.data.length; i++) acc = accum(acc, t.data[i]);\n  return finalize(acc, t.data.length);\n}\n\nfunction accum_dim(t, dim, init, accum, finalize) {\n  if (dim < 1) {\n    throw new Error(`reducer _dim: dim must be >= 1 (got ${dim})`);\n  }\n  if (dim > t.shape.length) {\n    // Reducing along a trailing singleton axis is the identity: every\n    // fiber has length 1, so sum/prod/mean each yield that single\n    // element unchanged. Return a same-shape COPY of the data (the C\n    // kernel memcpy\'s here too); allocating without copying left a\n    // zero-filled tensor — the original opt1 bug.\n    const out = mtoc2_tensor_alloc_nd(t.shape.length, t.shape.slice());\n    out.data.set(t.data);\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = t.shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= t.shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < t.shape.length; i++) after *= t.shape[i];\n  const outDims = squeeze_trailing(t.shape.slice());\n  outDims[dimIdx] = 1;\n  // Re-squeeze after the in-place axis update (the original `out_dims\n  // = a.shape.slice(); out_dims[dimIdx] = 1` then squeeze pattern).\n  squeeze_trailing(outDims);\n  const out = mtoc2_tensor_alloc_nd(outDims.length, outDims);\n  for (let aft = 0; aft < after; aft++) {\n    for (let bef = 0; bef < before; bef++) {\n      const base = aft * before * axis + bef;\n      let acc = init;\n      for (let k = 0; k < axis; k++) {\n        acc = accum(acc, t.data[base + k * before]);\n      }\n      out.data[aft * before + bef] = finalize(acc, axis);\n    }\n  }\n  return out;\n}\n\n// Min/max reducer. Ignores NaN like numbl/MATLAB: NaN entries are\n// skipped, and the result is NaN only when every element is NaN.\n// Mirrors the interpreter\'s `minMaxScan` (helpers/reduction/min-max.ts)\n// and the C kernel — seeding with data[0] would let a *leading* NaN\n// poison the result (`x < NaN` / `x > NaN` are always false).\nfunction minmax_all(t, op /* "min" | "max" */) {\n  if (t.data.length === 0) return op === "min" ? Infinity : -Infinity;\n  let best = NaN;\n  let found = false;\n  for (let i = 0; i < t.data.length; i++) {\n    const x = t.data[i];\n    if (x !== x) continue; // skip NaN\n    if (!found || (op === "min" ? x < best : x > best)) {\n      best = x;\n      found = true;\n    }\n  }\n  return found ? best : NaN;\n}\n\nfunction minmax_dim(t, dim, op) {\n  if (dim < 1) throw new Error(`reducer _dim: dim must be >= 1 (got ${dim})`);\n  if (dim > t.shape.length) {\n    const out = mtoc2_tensor_alloc_nd(t.shape.length, t.shape.slice());\n    out.data.set(t.data);\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = t.shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= t.shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < t.shape.length; i++) after *= t.shape[i];\n  const outDims = t.shape.slice();\n  outDims[dimIdx] = 1;\n  squeeze_trailing(outDims);\n  const out = mtoc2_tensor_alloc_nd(outDims.length, outDims);\n  for (let aft = 0; aft < after; aft++) {\n    for (let bef = 0; bef < before; bef++) {\n      const base = aft * before * axis + bef;\n      let best = NaN;\n      let found = false;\n      for (let k = 0; k < axis; k++) {\n        const x = t.data[base + k * before];\n        if (x !== x) continue; // skip NaN\n        if (!found || (op === "min" ? x < best : x > best)) {\n          best = x;\n          found = true;\n        }\n      }\n      out.data[aft * before + bef] = found ? best : NaN;\n    }\n  }\n  return out;\n}\n\n// Logical reducer (`any`, `all`). `emptyResult` is the value for a\n// 0-element reduction; `short` is the early-exit predicate.\nfunction logical_all(t, emptyResult, shortPredicate) {\n  if (t.data.length === 0) return emptyResult;\n  for (let i = 0; i < t.data.length; i++) {\n    if (shortPredicate(t.data[i])) return emptyResult === 1 ? 0 : 1;\n  }\n  return emptyResult;\n}\n\nfunction logical_dim(t, dim, emptyResult, shortPredicate) {\n  if (dim < 1) throw new Error(`reducer _dim: dim must be >= 1 (got ${dim})`);\n  if (dim > t.shape.length) {\n    // No-op axis: emit a logical cast of the input (each element →\n    // 1 if nonzero, 0 otherwise).\n    const out = mtoc2_tensor_alloc_nd(t.shape.length, t.shape.slice());\n    for (let i = 0; i < t.data.length; i++) {\n      out.data[i] = t.data[i] !== 0 ? 1 : 0;\n    }\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = t.shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= t.shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < t.shape.length; i++) after *= t.shape[i];\n  const outDims = t.shape.slice();\n  outDims[dimIdx] = 1;\n  squeeze_trailing(outDims);\n  const out = mtoc2_tensor_alloc_nd(outDims.length, outDims);\n  for (let aft = 0; aft < after; aft++) {\n    for (let bef = 0; bef < before; bef++) {\n      const base = aft * before * axis + bef;\n      let res = emptyResult;\n      for (let k = 0; k < axis; k++) {\n        if (shortPredicate(t.data[base + k * before])) {\n          res = emptyResult === 1 ? 0 : 1;\n          break;\n        }\n      }\n      out.data[aft * before + bef] = res;\n    }\n  }\n  return out;\n}\n\n// ── Sum ─────────────────────────────────────────────────────────────────\nconst sumInit = 0;\nconst sumAccum = (a, x) => a + x;\nconst idFinalize = a => a;\nconst mtoc2_sum_all = t => accum_all(t, sumInit, sumAccum, idFinalize);\nconst mtoc2_sum_dim = (t, d) =>\n  accum_dim(t, d, sumInit, sumAccum, idFinalize);\n\n// ── Prod ────────────────────────────────────────────────────────────────\nconst prodInit = 1;\nconst prodAccum = (a, x) => a * x;\nconst mtoc2_prod_all = t =>\n  accum_all(t, prodInit, prodAccum, idFinalize);\nconst mtoc2_prod_dim = (t, d) =>\n  accum_dim(t, d, prodInit, prodAccum, idFinalize);\n\n// ── Mean ────────────────────────────────────────────────────────────────\nconst meanFinalize = (a, n) => (n === 0 ? NaN : a / n);\nconst mtoc2_mean_all = t =>\n  accum_all(t, sumInit, sumAccum, meanFinalize);\nconst mtoc2_mean_dim = (t, d) =>\n  accum_dim(t, d, sumInit, sumAccum, meanFinalize);\n\n// ── Min / max ───────────────────────────────────────────────────────────\nconst mtoc2_min_all = t => minmax_all(t, "min");\nconst mtoc2_min_dim = (t, d) => minmax_dim(t, d, "min");\nconst mtoc2_max_all = t => minmax_all(t, "max");\nconst mtoc2_max_dim = (t, d) => minmax_dim(t, d, "max");\n\n// ── Any / all ───────────────────────────────────────────────────────────\n// any: short-circuits on a non-NaN nonzero; emptyResult = 0. NaN is\n// ignored (MATLAB: any(NaN) is 0, any([0 NaN]) is 0), so `x === x`\n// excludes it — without that, NaN wrongly short-circuited to true.\n// (`all` needs no such guard: allShort tests `x === 0`, which NaN\n// already fails, so a NaN simply doesn\'t force all to false.)\nconst anyShort = x => x !== 0 && x === x;\nconst mtoc2_any_all = t => logical_all(t, 0, anyShort);\nconst mtoc2_any_dim = (t, d) => logical_dim(t, d, 0, anyShort);\n// all: short-circuits on zero; emptyResult = 1.\nconst allShort = x => x === 0;\nconst mtoc2_all_all = t => logical_all(t, 1, allShort);\nconst mtoc2_all_dim = (t, d) => logical_dim(t, d, 1, allShort);\n',
  "tensor_repmat.js":
    "// JS sibling of `tensor_repmat.h`. Two helpers:\n//   - `mtoc2_tensor_repmat(in, nreps, reps)` — tile a real tensor.\n//   - `mtoc2_tensor_repmat_complex(in, nreps, reps)` — tile both\n//     lanes of a complex tensor (zero imag when input is real).\n// Negative reps clamp to 0; input shape and reps are right-padded\n// with 1s to a common rank.\n\n\n\nfunction mtoc2_tensor_repmat(input, nreps, repsIn) {\n  const reps = [];\n  for (let i = 0; i < nreps; i++) {\n    const r = repsIn[i] < 0 ? 0 : repsIn[i];\n    reps.push(r);\n  }\n  const inShape = input.shape;\n  const inNdim = inShape.length;\n  const outNdim = Math.max(nreps, inNdim);\n  const padShape = [];\n  const padReps = [];\n  const outDims = [];\n  for (let i = 0; i < outNdim; i++) {\n    padShape.push(i < inNdim ? inShape[i] : 1);\n    padReps.push(i < nreps ? reps[i] : 1);\n    outDims.push(padShape[i] * padReps[i]);\n  }\n  const out = mtoc2_tensor_alloc_nd(outNdim, outDims);\n  let outTotal = 1;\n  for (const d of outDims) outTotal *= d;\n  if (outTotal === 0) return out;\n  let inTotal = 1;\n  for (const d of inShape) inTotal *= d;\n  if (inTotal === 0) return out;\n\n  // Initial copy: trailing-1 padding doesn't change column-major layout.\n  out.data.set(input.data.subarray(0, inTotal), 0);\n\n  const curShape = padShape.slice();\n  let curTotal = inTotal;\n\n  for (let d = 0; d < outNdim; d++) {\n    const rep = padReps[d];\n    if (rep === 1) continue;\n    let blockSize = 1;\n    for (let i = 0; i <= d; i++) blockSize *= curShape[i];\n    if (rep === 0 || blockSize === 0) {\n      // outTotal will be 0; the alloc above already produced an empty\n      // tensor. Done.\n      return out;\n    }\n    const numBlocks = curTotal / blockSize;\n    // Walk blocks in reverse to avoid overwriting source data.\n    for (let b = numBlocks - 1; b >= 0; b--) {\n      const srcOff = b * blockSize;\n      const dstBase = b * blockSize * rep;\n      if (dstBase !== srcOff) {\n        // copyWithin handles overlapping moves correctly.\n        out.data.copyWithin(dstBase, srcOff, srcOff + blockSize);\n      }\n      for (let r = 1; r < rep; r++) {\n        out.data.copyWithin(\n          dstBase + r * blockSize,\n          dstBase,\n          dstBase + blockSize\n        );\n      }\n    }\n    curShape[d] *= rep;\n    curTotal *= rep;\n  }\n  return out;\n}\n\nfunction mtoc2_tensor_repmat_complex(input, nreps, repsIn) {\n  const reps = [];\n  for (let i = 0; i < nreps; i++) {\n    const r = repsIn[i] < 0 ? 0 : repsIn[i];\n    reps.push(r);\n  }\n  const inShape = input.shape;\n  const inNdim = inShape.length;\n  const outNdim = Math.max(nreps, inNdim);\n  const padShape = [];\n  const padReps = [];\n  const outDims = [];\n  for (let i = 0; i < outNdim; i++) {\n    padShape.push(i < inNdim ? inShape[i] : 1);\n    padReps.push(i < nreps ? reps[i] : 1);\n    outDims.push(padShape[i] * padReps[i]);\n  }\n  const out = mtoc2_tensor_alloc_nd_complex(outNdim, outDims);\n  let outTotal = 1;\n  for (const d of outDims) outTotal *= d;\n  if (outTotal === 0) return out;\n  let inTotal = 1;\n  for (const d of inShape) inTotal *= d;\n  if (inTotal === 0) return out;\n\n  const im = input.imag;\n  out.data.set(input.data.subarray(0, inTotal), 0);\n  if (im !== undefined) out.imag.set(im.subarray(0, inTotal), 0);\n\n  const curShape = padShape.slice();\n  let curTotal = inTotal;\n\n  for (let d = 0; d < outNdim; d++) {\n    const rep = padReps[d];\n    if (rep === 1) continue;\n    let blockSize = 1;\n    for (let i = 0; i <= d; i++) blockSize *= curShape[i];\n    if (rep === 0 || blockSize === 0) return out;\n    const numBlocks = curTotal / blockSize;\n    for (let b = numBlocks - 1; b >= 0; b--) {\n      const srcOff = b * blockSize;\n      const dstBase = b * blockSize * rep;\n      if (dstBase !== srcOff) {\n        out.data.copyWithin(dstBase, srcOff, srcOff + blockSize);\n        out.imag.copyWithin(dstBase, srcOff, srcOff + blockSize);\n      }\n      for (let r = 1; r < rep; r++) {\n        out.data.copyWithin(\n          dstBase + r * blockSize,\n          dstBase,\n          dstBase + blockSize\n        );\n        out.imag.copyWithin(\n          dstBase + r * blockSize,\n          dstBase,\n          dstBase + blockSize\n        );\n      }\n    }\n    curShape[d] *= rep;\n    curTotal *= rep;\n  }\n  return out;\n}\n",
  "tensor_reshape_nd.js":
    "// JS sibling of `tensor_reshape_nd.h`. Reshape a real tensor to an\n// N-D shape, supporting one `-1` auto-infer slot. Same error\n// behaviour as the C side: throws (instead of `abort()`) on bad\n// inputs.\n\n\nfunction mtoc2_reshape_nd(input, ndim, dims) {\n  let inTotal = 1;\n  for (const d of input.shape) inTotal *= d;\n  let inferIdx = -1;\n  let explicitProd = 1;\n  for (let i = 0; i < ndim; i++) {\n    if (dims[i] === -1) {\n      if (inferIdx !== -1) {\n        throw new Error(\"reshape: at most one '[]' auto-infer slot allowed\");\n      }\n      inferIdx = i;\n    } else if (dims[i] < 0) {\n      throw new Error(\n        `reshape: dim ${i + 1} must be a non-negative integer (got ${dims[i]})`\n      );\n    } else {\n      explicitProd *= dims[i];\n    }\n  }\n  const resolved = new Array(ndim);\n  for (let i = 0; i < ndim; i++) resolved[i] = dims[i];\n  let outTotal;\n  if (inferIdx !== -1) {\n    if (explicitProd === 0 && inTotal !== 0) {\n      throw new Error(\n        `reshape: input has ${inTotal} elements but explicit dims around '[]' multiply to 0`\n      );\n    }\n    if (explicitProd > 0 && inTotal % explicitProd !== 0) {\n      throw new Error(\n        `reshape: input has ${inTotal} elements, not divisible by ${explicitProd}`\n      );\n    }\n    resolved[inferIdx] = explicitProd === 0 ? 0 : inTotal / explicitProd;\n    outTotal = inTotal;\n  } else {\n    outTotal = explicitProd;\n    if (inTotal !== outTotal) {\n      throw new Error(\n        `reshape: number of elements must not change (in=${inTotal}, out=${outTotal})`\n      );\n    }\n  }\n  const out = mtoc2_tensor_alloc_nd(ndim, resolved);\n  if (outTotal > 0) out.data.set(input.data.subarray(0, outTotal));\n  return out;\n}\n",
  "tensor_reshape_nd_complex.js":
    "// JS sibling of `tensor_reshape_nd_complex.h`. Reshape a complex\n// tensor to an N-D shape. Same auto-infer / element-count rules as\n// the real reshape; both lanes are reinterpreted (no rearrangement).\n\n\nfunction mtoc2_reshape_nd_complex(input, ndim, dims) {\n  let inTotal = 1;\n  for (const d of input.shape) inTotal *= d;\n  let inferIdx = -1;\n  let explicitProd = 1;\n  for (let i = 0; i < ndim; i++) {\n    if (dims[i] === -1) {\n      if (inferIdx !== -1) {\n        throw new Error(\"reshape: at most one '[]' auto-infer slot allowed\");\n      }\n      inferIdx = i;\n    } else if (dims[i] < 0) {\n      throw new Error(\n        `reshape: dim ${i + 1} must be a non-negative integer (got ${dims[i]})`\n      );\n    } else {\n      explicitProd *= dims[i];\n    }\n  }\n  const resolved = new Array(ndim);\n  for (let i = 0; i < ndim; i++) resolved[i] = dims[i];\n  let outTotal;\n  if (inferIdx !== -1) {\n    if (explicitProd === 0 && inTotal !== 0) {\n      throw new Error(\n        `reshape: input has ${inTotal} elements but explicit dims around '[]' multiply to 0`\n      );\n    }\n    if (explicitProd > 0 && inTotal % explicitProd !== 0) {\n      throw new Error(\n        `reshape: input has ${inTotal} elements, not divisible by ${explicitProd}`\n      );\n    }\n    resolved[inferIdx] = explicitProd === 0 ? 0 : inTotal / explicitProd;\n    outTotal = inTotal;\n  } else {\n    outTotal = explicitProd;\n    if (inTotal !== outTotal) {\n      throw new Error(\n        `reshape: number of elements must not change (in=${inTotal}, out=${outTotal})`\n      );\n    }\n  }\n  const out = mtoc2_tensor_alloc_nd_complex(ndim, resolved);\n  if (outTotal > 0) {\n    out.data.set(input.data.subarray(0, outTotal));\n    if (input.imag !== undefined) {\n      out.imag.set(input.imag.subarray(0, outTotal));\n    }\n  }\n  return out;\n}\n",
  "tensor_size.js":
    "// JS sibling of `tensor_size.h`. Build a 1×ndim row tensor whose\n// elements are the input's dim sizes.\n\n\nfunction mtoc2_tensor_size_row(a) {\n  const n = a.shape.length;\n  const r = mtoc2_tensor_alloc(1, n);\n  for (let i = 0; i < n; i++) r.data[i] = a.shape[i];\n  return r;\n}\n",
  "tensor_sort_real.js":
    "// JS sibling of `tensor_sort_real.h`. Stable sort on real and\n// complex tensors. The descending flag flips the comparator while\n// keeping the tie-break on ascending original index. Complex sort\n// orders by magnitude then phase (matches numbl).\n\n\n\nfunction pair_sort_indices(a, descending) {\n  const n = a.data.length;\n  const idx = new Array(n);\n  for (let i = 0; i < n; i++) idx[i] = i;\n  idx.sort((p, q) => {\n    const av = a.data[p];\n    const bv = a.data[q];\n    // NaN ranks as the maximum (MATLAB): last when ascending, first\n    // when descending. Without this, NaN compares false both ways and\n    // falls through to the index tie-break, making the comparator\n    // non-transitive and corrupting the whole array.\n    const an = av !== av;\n    const bn = bv !== bv;\n    if (an || bn) {\n      if (an && bn) return p - q;\n      if (an) return descending ? -1 : 1;\n      return descending ? 1 : -1;\n    }\n    if (av < bv) return descending ? 1 : -1;\n    if (av > bv) return descending ? -1 : 1;\n    return p - q;\n  });\n  return idx;\n}\n\nfunction complex_sort_indices(a, descending) {\n  const n = a.data.length;\n  const im = a.imag;\n  const idx = new Array(n);\n  for (let i = 0; i < n; i++) idx[i] = i;\n\n  // All-zero imaginary lane → order by signed real value (matches the\n  // interpreter and MATLAB on real data), not by magnitude. NaNs sort\n  // last when ascending, first when descending.\n  let realMode = true;\n  if (im !== undefined) {\n    for (let i = 0; i < n; i++) {\n      if (im[i] !== 0) {\n        realMode = false;\n        break;\n      }\n    }\n  }\n  if (realMode) {\n    const re = a.data;\n    idx.sort((p, q) => {\n      const rp = re[p];\n      const rq = re[q];\n      const pNaN = rp !== rp;\n      const qNaN = rq !== rq;\n      if (pNaN && qNaN) return 0;\n      if (descending) {\n        if (pNaN) return -1;\n        if (qNaN) return 1;\n        return rp < rq ? 1 : rp > rq ? -1 : 0;\n      }\n      if (pNaN) return 1;\n      if (qNaN) return -1;\n      return rp < rq ? -1 : rp > rq ? 1 : 0;\n    });\n    return idx;\n  }\n\n  const mag = new Float64Array(n);\n  const ph = new Float64Array(n);\n  for (let i = 0; i < n; i++) {\n    const re = a.data[i];\n    const xi = im !== undefined ? im[i] : 0;\n    mag[i] = Math.hypot(re, xi);\n    ph[i] = Math.atan2(xi, re);\n  }\n  idx.sort((p, q) => {\n    if (mag[p] < mag[q]) return descending ? 1 : -1;\n    if (mag[p] > mag[q]) return descending ? -1 : 1;\n    if (ph[p] < ph[q]) return descending ? 1 : -1;\n    if (ph[p] > ph[q]) return descending ? -1 : 1;\n    return p - q;\n  });\n  return idx;\n}\n\nfunction mtoc2_sort_real(a, descending) {\n  const v = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  if (a.data.length === 0) return v;\n  const sorted = pair_sort_indices(a, descending);\n  for (let i = 0; i < sorted.length; i++) v.data[i] = a.data[sorted[i]];\n  return v;\n}\n\nfunction mtoc2_sort_real_2(a, descending) {\n  const v = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  const ix = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  if (a.data.length === 0) return { v, ix };\n  const sorted = pair_sort_indices(a, descending);\n  for (let i = 0; i < sorted.length; i++) {\n    v.data[i] = a.data[sorted[i]];\n    ix.data[i] = sorted[i] + 1;\n  }\n  return { v, ix };\n}\n\nfunction mtoc2_sort_complex(a, descending) {\n  const v = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  if (a.data.length === 0) return v;\n  const sorted = complex_sort_indices(a, descending);\n  const im = a.imag;\n  for (let i = 0; i < sorted.length; i++) {\n    v.data[i] = a.data[sorted[i]];\n    if (im !== undefined) v.imag[i] = im[sorted[i]];\n  }\n  return v;\n}\n\nfunction mtoc2_sort_complex_2(a, descending) {\n  const v = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  const ix = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  if (a.data.length === 0) return { v, ix };\n  const sorted = complex_sort_indices(a, descending);\n  const im = a.imag;\n  for (let i = 0; i < sorted.length; i++) {\n    v.data[i] = a.data[sorted[i]];\n    if (im !== undefined) v.imag[i] = im[sorted[i]];\n    ix.data[i] = sorted[i] + 1;\n  }\n  return { v, ix };\n}\n",
  "tensor_transpose.js":
    "// JS sibling of `tensor_transpose.h`. Real 2-D non-conjugate\n// transpose. Mirrors `transposeCore` semantics: column-major in,\n// column-major out.\n\n\nfunction mtoc2_tensor_transpose(a) {\n  const m = a.shape[0];\n  const n = a.shape[1];\n  const r = mtoc2_tensor_alloc(n, m);\n  for (let sc = 0; sc < n; sc++) {\n    for (let sr = 0; sr < m; sr++) {\n      r.data[sc + sr * n] = a.data[sr + sc * m];\n    }\n  }\n  return r;\n}\n",
  "tensor_transpose_complex.js":
    "// JS sibling of `tensor_transpose_complex.h`. Real 2-D non-conjugate\n// transpose for a complex tensor — both lanes get the same index\n// permutation. `'` (conjugate transpose) lowers to\n// `transpose(conj(z))` upstream, so this helper isn't responsible\n// for negating the imag lane.\n\n\nfunction mtoc2_tensor_transpose_complex(a) {\n  const m = a.shape[0];\n  const n = a.shape[1];\n  const r = mtoc2_tensor_alloc_complex(n, m);\n  const aim = a.imag;\n  for (let sc = 0; sc < n; sc++) {\n    for (let sr = 0; sr < m; sr++) {\n      r.data[sc + sr * n] = a.data[sr + sc * m];\n      r.imag[sc + sr * n] = aim !== undefined ? aim[sr + sc * m] : 0;\n    }\n  }\n  return r;\n}\n",
  "tensor_triangular.js":
    "// JS sibling of `tensor_triangular.h`. Four helpers: `triu` / `tril`\n// keep entries where `j - i >= k` / `i - j >= -k`; their `_complex`\n// siblings walk both lanes. Mirrors `triPart` in numbl's\n// `interpreter/builtins/array-extras.ts`.\n\n\n\nfunction mtoc2_tensor_triu(a, k) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const out = mtoc2_tensor_alloc(rows, cols);\n  for (let j = 0; j < cols; j++) {\n    for (let i = 0; i < rows; i++) {\n      if (j - i >= k) {\n        const idx = i + j * rows;\n        out.data[idx] = a.data[idx];\n      }\n    }\n  }\n  return out;\n}\n\nfunction mtoc2_tensor_tril(a, k) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const out = mtoc2_tensor_alloc(rows, cols);\n  for (let j = 0; j < cols; j++) {\n    for (let i = 0; i < rows; i++) {\n      if (i - j >= -k) {\n        const idx = i + j * rows;\n        out.data[idx] = a.data[idx];\n      }\n    }\n  }\n  return out;\n}\n\nfunction mtoc2_tensor_triu_complex(a, k) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const out = mtoc2_tensor_alloc_nd_complex(2, [rows, cols]);\n  const im = a.imag;\n  for (let j = 0; j < cols; j++) {\n    for (let i = 0; i < rows; i++) {\n      if (j - i >= k) {\n        const idx = i + j * rows;\n        out.data[idx] = a.data[idx];\n        if (im !== undefined) out.imag[idx] = im[idx];\n      }\n    }\n  }\n  return out;\n}\n\nfunction mtoc2_tensor_tril_complex(a, k) {\n  const rows = a.shape[0];\n  const cols = a.shape[1];\n  const out = mtoc2_tensor_alloc_nd_complex(2, [rows, cols]);\n  const im = a.imag;\n  for (let j = 0; j < cols; j++) {\n    for (let i = 0; i < rows; i++) {\n      if (i - j >= -k) {\n        const idx = i + j * rows;\n        out.data[idx] = a.data[idx];\n        if (im !== undefined) out.imag[idx] = im[idx];\n      }\n    }\n  }\n  return out;\n}\n",
  "tensor_unary_complex_math.js":
    "// JS sibling of `tensor_unary_complex_math.h`. Elementwise unary\n// math on complex tensors. Each helper allocates a fresh tensor\n// (real or complex, depending on the op) and walks every element\n// through the per-op `mtoc2_c*` scalar helper.\n\n\n\n\nfunction cUnaryLaneIm(t, i) {\n  return t.imag !== undefined ? t.imag[i] : 0;\n}\n\nfunction cUnary(a, fn) {\n  const r = mtoc2_tensor_alloc_nd_complex(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    const v = fn({ re: a.data[i], im: cUnaryLaneIm(a, i) });\n    r.data[i] = v.re;\n    r.imag[i] = v.im;\n  }\n  return r;\n}\n\nfunction mtoc2_tensor_sqrt_complex(a) {\n  return cUnary(a, mtoc2_csqrt);\n}\nfunction mtoc2_tensor_exp_complex(a) {\n  return cUnary(a, mtoc2_cexp);\n}\nfunction mtoc2_tensor_log_complex(a) {\n  return cUnary(a, mtoc2_clog);\n}\nfunction mtoc2_tensor_log2_complex(a) {\n  return cUnary(a, mtoc2_clog2);\n}\nfunction mtoc2_tensor_log10_complex(a) {\n  return cUnary(a, mtoc2_clog10);\n}\nfunction mtoc2_tensor_sin_complex(a) {\n  return cUnary(a, mtoc2_csin);\n}\nfunction mtoc2_tensor_cos_complex(a) {\n  return cUnary(a, mtoc2_ccos);\n}\nfunction mtoc2_tensor_tan_complex(a) {\n  return cUnary(a, mtoc2_ctan);\n}\nfunction mtoc2_tensor_atan_complex(a) {\n  return cUnary(a, mtoc2_catan);\n}\nfunction mtoc2_tensor_sinh_complex(a) {\n  return cUnary(a, mtoc2_csinh);\n}\nfunction mtoc2_tensor_cosh_complex(a) {\n  return cUnary(a, mtoc2_ccosh);\n}\nfunction mtoc2_tensor_tanh_complex(a) {\n  return cUnary(a, mtoc2_ctanh);\n}\nfunction mtoc2_tensor_floor_complex(a) {\n  return cUnary(a, mtoc2_cfloor);\n}\nfunction mtoc2_tensor_ceil_complex(a) {\n  return cUnary(a, mtoc2_cceil);\n}\nfunction mtoc2_tensor_round_complex(a) {\n  return cUnary(a, mtoc2_cround);\n}\nfunction mtoc2_tensor_fix_complex(a) {\n  return cUnary(a, mtoc2_cfix);\n}\nfunction mtoc2_tensor_sign_complex(a) {\n  return cUnary(a, mtoc2_csign);\n}\nfunction mtoc2_tensor_conj_complex(a) {\n  return cUnary(a, mtoc2_cconj);\n}\n\n// `imag`, `real`, `angle`, `abs` on a complex tensor all return a\n// REAL tensor (the imag lane is NOT allocated).\nfunction mtoc2_tensor_imag_complex(a) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = cUnaryLaneIm(a, i);\n  return r;\n}\nfunction mtoc2_tensor_real_complex(a) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = a.data[i];\n  return r;\n}\nfunction mtoc2_tensor_angle_complex(a) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    r.data[i] = Math.atan2(cUnaryLaneIm(a, i), a.data[i]);\n  }\n  return r;\n}\nfunction mtoc2_tensor_abs_complex(a) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) {\n    r.data[i] = Math.hypot(a.data[i], cUnaryLaneIm(a, i));\n  }\n  return r;\n}\n",
  "tensor_unary_real_math.js":
    "// JS sibling of `tensor_unary_real_math.h`. Per-op unary math\n// kernels for tensors (`mtoc2_tensor_sin`, `_cos`, etc.). Matches\n// the per-op function names the codegen emits.\n\n\nfunction unary_kernel(a, fn) {\n  const r = mtoc2_tensor_alloc_nd(a.shape.length, a.shape);\n  for (let i = 0; i < r.data.length; i++) r.data[i] = fn(a.data[i]);\n  return r;\n}\n\nfunction matlabRound(x) {\n  return Math.sign(x) * Math.round(Math.abs(x));\n}\n\nfunction mtoc2_tensor_cos(a) {\n  return unary_kernel(a, Math.cos);\n}\nfunction mtoc2_tensor_sin(a) {\n  return unary_kernel(a, Math.sin);\n}\nfunction mtoc2_tensor_tan(a) {\n  return unary_kernel(a, Math.tan);\n}\nfunction mtoc2_tensor_atan(a) {\n  return unary_kernel(a, Math.atan);\n}\nfunction mtoc2_tensor_sinh(a) {\n  return unary_kernel(a, Math.sinh);\n}\nfunction mtoc2_tensor_cosh(a) {\n  return unary_kernel(a, Math.cosh);\n}\nfunction mtoc2_tensor_tanh(a) {\n  return unary_kernel(a, Math.tanh);\n}\nfunction mtoc2_tensor_asinh(a) {\n  return unary_kernel(a, Math.asinh);\n}\nfunction mtoc2_tensor_exp(a) {\n  return unary_kernel(a, Math.exp);\n}\nfunction mtoc2_tensor_log(a) {\n  return unary_kernel(a, Math.log);\n}\nfunction mtoc2_tensor_log2(a) {\n  return unary_kernel(a, Math.log2);\n}\nfunction mtoc2_tensor_log10(a) {\n  return unary_kernel(a, Math.log10);\n}\nfunction mtoc2_tensor_sqrt(a) {\n  return unary_kernel(a, Math.sqrt);\n}\nfunction mtoc2_tensor_abs(a) {\n  return unary_kernel(a, Math.abs);\n}\nfunction mtoc2_tensor_floor(a) {\n  return unary_kernel(a, Math.floor);\n}\nfunction mtoc2_tensor_ceil(a) {\n  return unary_kernel(a, Math.ceil);\n}\nfunction mtoc2_tensor_fix(a) {\n  return unary_kernel(a, Math.trunc);\n}\nfunction mtoc2_tensor_round(a) {\n  return unary_kernel(a, matlabRound);\n}\nfunction mtoc2_tensor_sign(a) {\n  return unary_kernel(a, Math.sign);\n}\n",
  "tensor_uniquetol.js":
    "// JS sibling of tensor_uniquetol.h. Same naive pairwise-against-\n// running-list scan, preserving first-occurrence order and the\n// transitive-chaining behaviour that follows from it.\n\n\nfunction mtoc2_uniquetol_real(a, tol, rowOut) {\n  const n = a.data.length;\n  const uniques = [];\n  for (let i = 0; i < n; i++) {\n    const x = a.data[i];\n    let found = false;\n    for (let k = 0; k < uniques.length; k++) {\n      const u = uniques[k];\n      if (Number.isNaN(u) || Number.isNaN(x)) continue;\n      if (Math.abs(x - u) <= tol) {\n        found = true;\n        break;\n      }\n    }\n    if (!found) uniques.push(x);\n  }\n  const shape = rowOut ? [1, uniques.length] : [uniques.length, 1];\n  const r = mtoc2_tensor_alloc_nd(2, shape);\n  for (let i = 0; i < uniques.length; i++) r.data[i] = uniques[i];\n  return r;\n}\n",
  "tensor_var.js":
    "// JS sibling of `tensor_var.h`. Variance / standard-deviation\n// reductions, byte-for-byte with numbl's `varianceOf`\n// (interpreter/builtins/reductions.ts) — including the `** 2`\n// squared-deviation so opt0 (interpreter) and opt1 (this kernel)\n// agree exactly. Helper names are prefixed to avoid colliding with\n// `tensor_reduce_real.js`'s `squeeze_trailing` when both snippets are\n// inlined into one generated module.\n\n\nfunction var_squeeze_trailing(dims) {\n  while (dims.length > 2 && dims[dims.length - 1] === 1) dims.pop();\n  return dims;\n}\n\nfunction variance_fiber(data, base, n, stride, w) {\n  if (n === 0) return NaN;\n  if (n <= 1 && w === 0) return 0;\n  let s = 0;\n  for (let k = 0; k < n; k++) s += data[base + k * stride];\n  const m = s / n;\n  let ss = 0;\n  for (let k = 0; k < n; k++) ss += (data[base + k * stride] - m) ** 2;\n  return ss / (w === 1 ? n : n - 1);\n}\n\nfunction var_all_impl(t, w, transform) {\n  return transform(variance_fiber(t.data, 0, t.data.length, 1, w));\n}\n\nfunction var_dim_impl(t, dim, w, transform) {\n  if (dim < 1) throw new Error(`var/std _dim: dim must be >= 1 (got ${dim})`);\n  if (dim > t.shape.length) {\n    // No-op axis: numbl's reduceDim copies the input verbatim when\n    // dim > ndim (forEachSlice returns null), regardless of the\n    // reducer — so var/std return the input unchanged, not zeros.\n    const out = mtoc2_tensor_alloc_nd(t.shape.length, t.shape.slice());\n    out.data.set(t.data);\n    return out;\n  }\n  const dimIdx = dim - 1;\n  const axis = t.shape[dimIdx];\n  let before = 1;\n  for (let i = 0; i < dimIdx; i++) before *= t.shape[i];\n  let after = 1;\n  for (let i = dimIdx + 1; i < t.shape.length; i++) after *= t.shape[i];\n  const outDims = t.shape.slice();\n  outDims[dimIdx] = 1;\n  var_squeeze_trailing(outDims);\n  const out = mtoc2_tensor_alloc_nd(outDims.length, outDims);\n  for (let aft = 0; aft < after; aft++) {\n    for (let bef = 0; bef < before; bef++) {\n      const base = aft * before * axis + bef;\n      out.data[aft * before + bef] = transform(\n        variance_fiber(t.data, base, axis, before, w)\n      );\n    }\n  }\n  return out;\n}\n\nfunction mtoc2_var_all(t, w) {\n  return var_all_impl(t, w, x => x);\n}\nfunction mtoc2_var_dim(t, dim, w) {\n  return var_dim_impl(t, dim, w, x => x);\n}\nfunction mtoc2_std_all(t, w) {\n  return var_all_impl(t, w, Math.sqrt);\n}\nfunction mtoc2_std_dim(t, dim, w) {\n  return var_dim_impl(t, dim, w, Math.sqrt);\n}\n",
  "tensor_zeros_nd.js":
    "// JS sibling of `tensor_zeros_nd.h`. Float64Array initialises to\n// zeros automatically, so `_zeros_nd` is just an alias for the\n// allocator.\n\n\nfunction mtoc2_tensor_zeros_nd(ndim, dims) {\n  return mtoc2_tensor_alloc_nd(ndim, dims);\n}\n",
  "tensor_zeros_square.js":
    "// JS sibling of `tensor_zeros_square.h`. Single-eval helper for\n// `zeros(n)` with a runtime `n` — keeps the source-level expression\n// from being evaluated twice (the C side passes `n` as a parameter\n// to avoid macro-style double-evaluation; the JS analogue keeps the\n// parallel call shape so the emitter stays structurally aligned).\n\n\nfunction mtoc2_tensor_zeros_square(n) {\n  return mtoc2_tensor_zeros_nd(2, [n, n]);\n}\n",
  "strcmp.js":
    '/* JS sibling of strcmp.h — input shape mirrors what mtoc2\'s js-aot /\n * interpreter pass to the helper: either a raw JS string (numbl-style\n * scalar handle) or a `{mtoc2Tag:"char", value:string}` char wrapper.\n * The helper normalizes either form to its raw bytes before comparing.\n */\n\nfunction _mtoc2_strcmp_text(v) {\n  if (typeof v === "string") return v;\n  if (v && v.mtoc2Tag === "char" && typeof v.value === "string") return v.value;\n  return null;\n}\n\nfunction mtoc2_strcmp(a, b) {\n  const sa = _mtoc2_strcmp_text(a);\n  const sb = _mtoc2_strcmp_text(b);\n  if (sa === null || sb === null) return 0;\n  return sa === sb ? 1 : 0;\n}\n\nfunction mtoc2_strcmpi(a, b) {\n  const sa = _mtoc2_strcmp_text(a);\n  const sb = _mtoc2_strcmp_text(b);\n  if (sa === null || sb === null) return 0;\n  return sa.toLowerCase() === sb.toLowerCase() ? 1 : 0;\n}\n',
};

/** Per-JS-snippet list of basenames it imports from sibling JS
 *  snippets (e.g. `tensor_elemwise_real.js` →
 *  `['tensor_alloc_nd.js']`). The codegen path converts each entry
 *  to a registered snippet name (`mtoc2_<basename without .js>`) so
 *  cross-snippet JS deps activate alongside the explicit C deps. */
export const JS_IMPORTS: Record<string, string[]> = {
  "cell.js": ["format_double.js"],
  "loop_count.js": [],
  "range_value.js": [],
  "scalar_index.js": [],
  "tensor_make_range.js": [
    "loop_count.js",
    "range_value.js",
    "tensor_alloc_nd.js",
  ],
  "assert_fmt.js": ["format_engine.js"],
  "disp_complex.js": ["format_complex.js"],
  "disp_double.js": ["format_double.js"],
  "disp_struct.js": ["disp_double.js", "disp_tensor.js"],
  "disp_tensor.js": ["format_double.js"],
  "disp_tensor_complex.js": ["format_complex.js"],
  "error_fmt.js": ["format_engine.js"],
  "format_complex.js": ["format_double.js"],
  "format_double.js": [],
  "format_engine.js": [],
  "fprintf.js": ["format_engine.js"],
  "sprintf.js": ["format_engine.js"],
  "warning_fmt.js": ["format_engine.js"],
  "plot_dispatch.js": [],
  "cdiv.js": [],
  "cscalar.js": [],
  "deep_clone.js": [],
  "isequal.js": [],
  "tictoc.js": [],
  "tensor.js": [],
  "tensor_alloc.js": ["tensor.js"],
  "tensor_alloc_complex.js": [],
  "tensor_alloc_nd.js": ["tensor.js"],
  "tensor_alloc_nd_complex.js": [],
  "tensor_copy_complex.js": [],
  "tensor_from_matrix.js": ["tensor_alloc.js"],
  "tensor_from_matrix_complex.js": ["tensor_alloc_complex.js"],
  "tensor_from_row.js": ["tensor_alloc.js"],
  "tensor_from_row_complex.js": ["tensor_alloc_complex.js"],
  "tensor_cat.js": ["tensor_alloc_nd.js", "tensor_alloc_nd_complex.js"],
  "tensor_cumulative.js": ["tensor_alloc_nd.js", "tensor_alloc_nd_complex.js"],
  "tensor_diag.js": ["tensor_alloc.js", "tensor_alloc_nd_complex.js"],
  "tensor_diff.js": ["tensor_alloc_nd.js"],
  "tensor_dot.js": ["tensor_alloc_nd.js", "tensor_alloc_nd_complex.js"],
  "tensor_elemwise_complex.js": ["cdiv.js", "tensor_alloc_nd_complex.js"],
  "tensor_elemwise_real.js": ["tensor_alloc_nd.js"],
  "tensor_elemwise_real_fn.js": ["tensor_alloc_nd.js"],
  "tensor_eye.js": ["tensor_alloc.js"],
  "tensor_fill_nd.js": ["tensor_alloc_nd.js", "tensor_alloc_nd_complex.js"],
  "tensor_fill_square.js": ["tensor_fill_nd.js"],
  "tensor_flip.js": ["tensor_alloc_nd.js", "tensor_alloc_nd_complex.js"],
  "tensor_imag_all_zero.js": [],
  "tensor_linspace.js": ["tensor_alloc.js"],
  "tensor_logical_real.js": ["tensor_alloc_nd.js"],
  "tensor_logspace.js": ["tensor_alloc.js"],
  "tensor_meshgrid.js": ["tensor_alloc.js"],
  "tensor_mtimes_complex.js": ["tensor_alloc_complex.js"],
  "tensor_mtimes_real.js": ["tensor_alloc.js"],
  "tensor_norm.js": [],
  "tensor_ones_nd.js": ["tensor_alloc_nd.js"],
  "tensor_ones_square.js": ["tensor_ones_nd.js"],
  "tensor_predicate.js": [],
  "tensor_reduce_complex.js": [
    "tensor_alloc_nd.js",
    "tensor_alloc_nd_complex.js",
  ],
  "tensor_reduce_real.js": ["tensor_alloc_nd.js"],
  "tensor_repmat.js": ["tensor_alloc_nd.js", "tensor_alloc_nd_complex.js"],
  "tensor_reshape_nd.js": ["tensor_alloc_nd.js"],
  "tensor_reshape_nd_complex.js": ["tensor_alloc_nd_complex.js"],
  "tensor_size.js": ["tensor_alloc.js"],
  "tensor_sort_real.js": ["tensor_alloc_nd.js", "tensor_alloc_nd_complex.js"],
  "tensor_transpose.js": ["tensor_alloc.js"],
  "tensor_transpose_complex.js": ["tensor_alloc_complex.js"],
  "tensor_triangular.js": ["tensor_alloc.js", "tensor_alloc_nd_complex.js"],
  "tensor_unary_complex_math.js": [
    "tensor_alloc_nd.js",
    "tensor_alloc_nd_complex.js",
  ],
  "tensor_unary_real_math.js": ["tensor_alloc_nd.js"],
  "tensor_uniquetol.js": ["tensor_alloc_nd.js"],
  "tensor_var.js": ["tensor_alloc_nd.js"],
  "tensor_zeros_nd.js": ["tensor_alloc_nd.js"],
  "tensor_zeros_square.js": ["tensor_zeros_nd.js"],
  "strcmp.js": [],
};
