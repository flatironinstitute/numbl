classdef PrefStoreParent_
% Parent class that defines the prefList property (like chebpref).
% The key pattern: the property is defined in the PARENT, not the child.
    properties
        prefList
    end
    methods
    end
end
