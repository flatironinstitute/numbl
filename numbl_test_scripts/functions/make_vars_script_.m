% A script (no function header). When invoked by name it must run in the
% caller's workspace, so these variables become visible to the caller. Helper
% for script_shares_caller_workspace.m; not a standalone test.
sv_alpha = 11;
sv_beta = [2 4 6];
